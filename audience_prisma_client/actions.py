# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse

if TYPE_CHECKING:
    from .client import Client
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class AudienceRoomActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.AudienceRoom]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await AudienceRoom.prisma().query_raw(
            'SELECT * FROM AudienceRoom WHERE id = $1',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.AudienceRoom
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await AudienceRoom.prisma().query_first(
            'SELECT * FROM AudienceRoom WHERE name = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AudienceRoomCreateInput,
        include: Optional[types.AudienceRoomInclude] = None
    ) -> _PrismaModelT:
        """Create a new AudienceRoom record.

        Parameters
        ----------
        data
            AudienceRoom record data
        include
            Specifies which relations should be loaded on the returned AudienceRoom model

        Returns
        -------
        prisma.models.AudienceRoom
            The created AudienceRoom record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a AudienceRoom record from just the required fields
        audienceroom = await AudienceRoom.prisma().create(
            data={
                # data to create a AudienceRoom record
                'name': 'ggciceaie',
                'descriptionS3Url': 'bbehjachib',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AudienceRoomCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple AudienceRoom records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of AudienceRoom record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await AudienceRoom.prisma().create_many(
            data=[
                {
                    # data to create a AudienceRoom record
                    'name': 'cadfabfehe',
                    'descriptionS3Url': 'dgiiaaijj',
                },
                {
                    # data to create a AudienceRoom record
                    'name': 'bfaiacjjfc',
                    'descriptionS3Url': 'eigcfgbif',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AudienceRoomWhereUniqueInput,
        include: Optional[types.AudienceRoomInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single AudienceRoom record.

        Parameters
        ----------
        where
            AudienceRoom filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned AudienceRoom model

        Returns
        -------
        prisma.models.AudienceRoom
            The deleted AudienceRoom record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audienceroom = await AudienceRoom.prisma().delete(
            where={
                'id': 'bagcfbhiig',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AudienceRoomWhereUniqueInput,
        include: Optional[types.AudienceRoomInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique AudienceRoom record.

        Parameters
        ----------
        where
            AudienceRoom filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AudienceRoom model

        Returns
        -------
        prisma.models.AudienceRoom
            The found AudienceRoom record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audienceroom = await AudienceRoom.prisma().find_unique(
            where={
                'id': 'cghideieh',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AudienceRoomWhereUniqueInput,
        include: Optional[types.AudienceRoomInclude] = None
    ) -> _PrismaModelT:
        """Find a unique AudienceRoom record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            AudienceRoom filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AudienceRoom model

        Returns
        -------
        prisma.models.AudienceRoom
            The found AudienceRoom record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audienceroom = await AudienceRoom.prisma().find_unique_or_raise(
            where={
                'id': 'biabhbdai',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AudienceRoomWhereInput] = None,
        cursor: Optional[types.AudienceRoomWhereUniqueInput] = None,
        include: Optional[types.AudienceRoomInclude] = None,
        order: Optional[Union[types.AudienceRoomOrderByInput, List[types.AudienceRoomOrderByInput]]] = None,
        distinct: Optional[List[types.AudienceRoomScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple AudienceRoom records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of AudienceRoom records returned
        skip
            Ignore the first N results
        where
            AudienceRoom filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AudienceRoom model
        order
            Order the returned AudienceRoom records by any field
        distinct
            Filter AudienceRoom records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.AudienceRoom]
            The list of all AudienceRoom records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 AudienceRoom records
        audiencerooms = await AudienceRoom.prisma().find_many(take=10)

        # find the first 5 AudienceRoom records ordered by the descriptionS3Url field
        audiencerooms = await AudienceRoom.prisma().find_many(
            take=5,
            order={
                'descriptionS3Url': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AudienceRoomWhereInput] = None,
        cursor: Optional[types.AudienceRoomWhereUniqueInput] = None,
        include: Optional[types.AudienceRoomInclude] = None,
        order: Optional[Union[types.AudienceRoomOrderByInput, List[types.AudienceRoomOrderByInput]]] = None,
        distinct: Optional[List[types.AudienceRoomScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single AudienceRoom record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AudienceRoom filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AudienceRoom model
        order
            Order the returned AudienceRoom records by any field
        distinct
            Filter AudienceRoom records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AudienceRoom
            The first AudienceRoom record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AudienceRoom record ordered by the createdAt field
        audienceroom = await AudienceRoom.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AudienceRoomWhereInput] = None,
        cursor: Optional[types.AudienceRoomWhereUniqueInput] = None,
        include: Optional[types.AudienceRoomInclude] = None,
        order: Optional[Union[types.AudienceRoomOrderByInput, List[types.AudienceRoomOrderByInput]]] = None,
        distinct: Optional[List[types.AudienceRoomScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single AudienceRoom record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AudienceRoom filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AudienceRoom model
        order
            Order the returned AudienceRoom records by any field
        distinct
            Filter AudienceRoom records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AudienceRoom
            The first AudienceRoom record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AudienceRoom record ordered by the updatedAt field
        audienceroom = await AudienceRoom.prisma().find_first_or_raise(
            skip=1,
            order={
                'updatedAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AudienceRoomUpdateInput,
        where: types.AudienceRoomWhereUniqueInput,
        include: Optional[types.AudienceRoomInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single AudienceRoom record.

        Parameters
        ----------
        data
            AudienceRoom record data specifying what to update
        where
            AudienceRoom filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned AudienceRoom model

        Returns
        -------
        prisma.models.AudienceRoom
            The updated AudienceRoom record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        audienceroom = await AudienceRoom.prisma().update(
            where={
                'id': 'idghgaicb',
            },
            data={
                # data to update the AudienceRoom record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AudienceRoomWhereUniqueInput,
        data: types.AudienceRoomUpsertInput,
        include: Optional[types.AudienceRoomInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            AudienceRoom filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned AudienceRoom model

        Returns
        -------
        prisma.models.AudienceRoom
            The created or updated AudienceRoom record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audienceroom = await AudienceRoom.prisma().upsert(
            where={
                'id': 'fjfddhigg',
            },
            data={
                'create': {
                    'id': 'fjfddhigg',
                    'name': 'bfaiacjjfc',
                    'descriptionS3Url': 'eigcfgbif',
                },
                'update': {
                    'name': 'bfaiacjjfc',
                    'descriptionS3Url': 'eigcfgbif',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AudienceRoomUpdateManyMutationInput,
        where: types.AudienceRoomWhereInput,
    ) -> int:
        """Update multiple AudienceRoom records

        Parameters
        ----------
        data
            AudienceRoom data to update the selected AudienceRoom records to
        where
            Filter to select the AudienceRoom records to update

        Returns
        -------
        int
            The total number of AudienceRoom records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all AudienceRoom records
        total = await AudienceRoom.prisma().update_many(
            data={
                'userId': 'hjaecfifb'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AudienceRoomWhereInput] = None,
        cursor: Optional[types.AudienceRoomWhereUniqueInput] = None,
    ) -> int:
        """Count the number of AudienceRoom records present in the database

        Parameters
        ----------
        select
            Select the AudienceRoom fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AudienceRoom filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AudienceRoomCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await AudienceRoom.prisma().count()

        # results: prisma.types.AudienceRoomCountAggregateOutput
        results = await AudienceRoom.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AudienceRoomCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AudienceRoomWhereInput] = None,
        cursor: Optional[types.AudienceRoomWhereUniqueInput] = None,
    ) -> types.AudienceRoomCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AudienceRoomCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AudienceRoomWhereInput] = None,
        cursor: Optional[types.AudienceRoomWhereUniqueInput] = None,
    ) -> Union[int, types.AudienceRoomCountAggregateOutput]:
        """Count the number of AudienceRoom records present in the database

        Parameters
        ----------
        select
            Select the AudienceRoom fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AudienceRoom filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AudienceRoomCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await AudienceRoom.prisma().count()

        # results: prisma.types.AudienceRoomCountAggregateOutput
        results = await AudienceRoom.prisma().count(
            select={
                '_all': True,
                'name': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AudienceRoomCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AudienceRoomWhereInput] = None
    ) -> int:
        """Delete multiple AudienceRoom records.

        Parameters
        ----------
        where
            Optional AudienceRoom filter to find the records to be deleted

        Returns
        -------
        int
            The total number of AudienceRoom records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all AudienceRoom records
        total = await AudienceRoom.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AudienceRoomScalarFieldKeys'],
        *,
        where: Optional['types.AudienceRoomWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AudienceRoomAvgAggregateInput'] = None,
        sum: Optional['types.AudienceRoomSumAggregateInput'] = None,
        min: Optional['types.AudienceRoomMinAggregateInput'] = None,
        max: Optional['types.AudienceRoomMaxAggregateInput'] = None,
        having: Optional['types.AudienceRoomScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AudienceRoomCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AudienceRoomScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AudienceRoomScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AudienceRoomGroupByOutput']:
        """Group AudienceRoom records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar AudienceRoom fields to group records by
        where
            AudienceRoom filter to select records
        take
            Limit the maximum number of AudienceRoom records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AudienceRoomGroupByOutput]
            A list of dictionaries representing the AudienceRoom record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group AudienceRoom records by descriptionS3Url values
        # and count how many records are in each group
        results = await AudienceRoom.prisma().group_by(
            ['descriptionS3Url'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class AudienceProfileActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.AudienceProfile]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await AudienceProfile.prisma().query_raw(
            'SELECT * FROM AudienceProfile WHERE id = $1',
            'cbbbjbfcii',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.AudienceProfile
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await AudienceProfile.prisma().query_first(
            'SELECT * FROM AudienceProfile WHERE audienceRoomId = $1',
            'bbejhfidcb',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.AudienceProfileCreateInput,
        include: Optional[types.AudienceProfileInclude] = None
    ) -> _PrismaModelT:
        """Create a new AudienceProfile record.

        Parameters
        ----------
        data
            AudienceProfile record data
        include
            Specifies which relations should be loaded on the returned AudienceProfile model

        Returns
        -------
        prisma.models.AudienceProfile
            The created AudienceProfile record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a AudienceProfile record from just the required fields
        audienceprofile = await AudienceProfile.prisma().create(
            data={
                # data to create a AudienceProfile record
                'audienceRoomId': 'bgeecijdgg',
                'profileName': 'bdiicjafbj',
                'linkedinUrl': 'bgehebiafc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.AudienceProfileCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple AudienceProfile records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of AudienceProfile record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await AudienceProfile.prisma().create_many(
            data=[
                {
                    # data to create a AudienceProfile record
                    'audienceRoomId': 'bghffegacj',
                    'profileName': 'bhghchehcc',
                    'linkedinUrl': 'dcgchcbbf',
                },
                {
                    # data to create a AudienceProfile record
                    'audienceRoomId': 'bdedcabahc',
                    'profileName': 'ghfhiafcb',
                    'linkedinUrl': 'heejgedji',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.AudienceProfileWhereUniqueInput,
        include: Optional[types.AudienceProfileInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single AudienceProfile record.

        Parameters
        ----------
        where
            AudienceProfile filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned AudienceProfile model

        Returns
        -------
        prisma.models.AudienceProfile
            The deleted AudienceProfile record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audienceprofile = await AudienceProfile.prisma().delete(
            where={
                'id': 'bjgjgibgbf',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.AudienceProfileWhereUniqueInput,
        include: Optional[types.AudienceProfileInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique AudienceProfile record.

        Parameters
        ----------
        where
            AudienceProfile filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AudienceProfile model

        Returns
        -------
        prisma.models.AudienceProfile
            The found AudienceProfile record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audienceprofile = await AudienceProfile.prisma().find_unique(
            where={
                'id': 'bbbgbhfjge',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.AudienceProfileWhereUniqueInput,
        include: Optional[types.AudienceProfileInclude] = None
    ) -> _PrismaModelT:
        """Find a unique AudienceProfile record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            AudienceProfile filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned AudienceProfile model

        Returns
        -------
        prisma.models.AudienceProfile
            The found AudienceProfile record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audienceprofile = await AudienceProfile.prisma().find_unique_or_raise(
            where={
                'id': 'igbehcbab',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AudienceProfileWhereInput] = None,
        cursor: Optional[types.AudienceProfileWhereUniqueInput] = None,
        include: Optional[types.AudienceProfileInclude] = None,
        order: Optional[Union[types.AudienceProfileOrderByInput, List[types.AudienceProfileOrderByInput]]] = None,
        distinct: Optional[List[types.AudienceProfileScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple AudienceProfile records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of AudienceProfile records returned
        skip
            Ignore the first N results
        where
            AudienceProfile filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AudienceProfile model
        order
            Order the returned AudienceProfile records by any field
        distinct
            Filter AudienceProfile records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.AudienceProfile]
            The list of all AudienceProfile records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 AudienceProfile records
        audienceprofiles = await AudienceProfile.prisma().find_many(take=10)

        # find the first 5 AudienceProfile records ordered by the profileName field
        audienceprofiles = await AudienceProfile.prisma().find_many(
            take=5,
            order={
                'profileName': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AudienceProfileWhereInput] = None,
        cursor: Optional[types.AudienceProfileWhereUniqueInput] = None,
        include: Optional[types.AudienceProfileInclude] = None,
        order: Optional[Union[types.AudienceProfileOrderByInput, List[types.AudienceProfileOrderByInput]]] = None,
        distinct: Optional[List[types.AudienceProfileScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single AudienceProfile record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AudienceProfile filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AudienceProfile model
        order
            Order the returned AudienceProfile records by any field
        distinct
            Filter AudienceProfile records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AudienceProfile
            The first AudienceProfile record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AudienceProfile record ordered by the linkedinUrl field
        audienceprofile = await AudienceProfile.prisma().find_first(
            skip=1,
            order={
                'linkedinUrl': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.AudienceProfileWhereInput] = None,
        cursor: Optional[types.AudienceProfileWhereUniqueInput] = None,
        include: Optional[types.AudienceProfileInclude] = None,
        order: Optional[Union[types.AudienceProfileOrderByInput, List[types.AudienceProfileOrderByInput]]] = None,
        distinct: Optional[List[types.AudienceProfileScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single AudienceProfile record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            AudienceProfile filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned AudienceProfile model
        order
            Order the returned AudienceProfile records by any field
        distinct
            Filter AudienceProfile records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.AudienceProfile
            The first AudienceProfile record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second AudienceProfile record ordered by the profileDescriptionS3Url field
        audienceprofile = await AudienceProfile.prisma().find_first_or_raise(
            skip=1,
            order={
                'profileDescriptionS3Url': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.AudienceProfileUpdateInput,
        where: types.AudienceProfileWhereUniqueInput,
        include: Optional[types.AudienceProfileInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single AudienceProfile record.

        Parameters
        ----------
        data
            AudienceProfile record data specifying what to update
        where
            AudienceProfile filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned AudienceProfile model

        Returns
        -------
        prisma.models.AudienceProfile
            The updated AudienceProfile record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        audienceprofile = await AudienceProfile.prisma().update(
            where={
                'id': 'bdadaadhag',
            },
            data={
                # data to update the AudienceProfile record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.AudienceProfileWhereUniqueInput,
        data: types.AudienceProfileUpsertInput,
        include: Optional[types.AudienceProfileInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            AudienceProfile filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned AudienceProfile model

        Returns
        -------
        prisma.models.AudienceProfile
            The created or updated AudienceProfile record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        audienceprofile = await AudienceProfile.prisma().upsert(
            where={
                'id': 'bgiggdidbf',
            },
            data={
                'create': {
                    'id': 'bgiggdidbf',
                    'audienceRoomId': 'bdedcabahc',
                    'profileName': 'ghfhiafcb',
                    'linkedinUrl': 'heejgedji',
                },
                'update': {
                    'audienceRoomId': 'bdedcabahc',
                    'profileName': 'ghfhiafcb',
                    'linkedinUrl': 'heejgedji',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.AudienceProfileUpdateManyMutationInput,
        where: types.AudienceProfileWhereInput,
    ) -> int:
        """Update multiple AudienceProfile records

        Parameters
        ----------
        data
            AudienceProfile data to update the selected AudienceProfile records to
        where
            Filter to select the AudienceProfile records to update

        Returns
        -------
        int
            The total number of AudienceProfile records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all AudienceProfile records
        total = await AudienceProfile.prisma().update_many(
            data={
                'postsS3Url': 'caaaedabfc'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AudienceProfileWhereInput] = None,
        cursor: Optional[types.AudienceProfileWhereUniqueInput] = None,
    ) -> int:
        """Count the number of AudienceProfile records present in the database

        Parameters
        ----------
        select
            Select the AudienceProfile fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AudienceProfile filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AudienceProfileCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await AudienceProfile.prisma().count()

        # results: prisma.types.AudienceProfileCountAggregateOutput
        results = await AudienceProfile.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.AudienceProfileCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AudienceProfileWhereInput] = None,
        cursor: Optional[types.AudienceProfileWhereUniqueInput] = None,
    ) -> types.AudienceProfileCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.AudienceProfileCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.AudienceProfileWhereInput] = None,
        cursor: Optional[types.AudienceProfileWhereUniqueInput] = None,
    ) -> Union[int, types.AudienceProfileCountAggregateOutput]:
        """Count the number of AudienceProfile records present in the database

        Parameters
        ----------
        select
            Select the AudienceProfile fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            AudienceProfile filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.AudienceProfileCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await AudienceProfile.prisma().count()

        # results: prisma.types.AudienceProfileCountAggregateOutput
        results = await AudienceProfile.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.AudienceProfileCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.AudienceProfileWhereInput] = None
    ) -> int:
        """Delete multiple AudienceProfile records.

        Parameters
        ----------
        where
            Optional AudienceProfile filter to find the records to be deleted

        Returns
        -------
        int
            The total number of AudienceProfile records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all AudienceProfile records
        total = await AudienceProfile.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.AudienceProfileScalarFieldKeys'],
        *,
        where: Optional['types.AudienceProfileWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.AudienceProfileAvgAggregateInput'] = None,
        sum: Optional['types.AudienceProfileSumAggregateInput'] = None,
        min: Optional['types.AudienceProfileMinAggregateInput'] = None,
        max: Optional['types.AudienceProfileMaxAggregateInput'] = None,
        having: Optional['types.AudienceProfileScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.AudienceProfileCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.AudienceProfileScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.AudienceProfileScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.AudienceProfileGroupByOutput']:
        """Group AudienceProfile records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar AudienceProfile fields to group records by
        where
            AudienceProfile filter to select records
        take
            Limit the maximum number of AudienceProfile records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.AudienceProfileGroupByOutput]
            A list of dictionaries representing the AudienceProfile record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group AudienceProfile records by id values
        # and count how many records are in each group
        results = await AudienceProfile.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PostClassifierActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.PostClassifier]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await PostClassifier.prisma().query_raw(
            'SELECT * FROM PostClassifier WHERE id = $1',
            'bigibebcib',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.PostClassifier
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await PostClassifier.prisma().query_first(
            'SELECT * FROM PostClassifier WHERE userId = $1',
            'bigaiehgcc',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.PostClassifierCreateInput,
        include: Optional[types.PostClassifierInclude] = None
    ) -> _PrismaModelT:
        """Create a new PostClassifier record.

        Parameters
        ----------
        data
            PostClassifier record data
        include
            Specifies which relations should be loaded on the returned PostClassifier model

        Returns
        -------
        prisma.models.PostClassifier
            The created PostClassifier record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a PostClassifier record from just the required fields
        postclassifier = await PostClassifier.prisma().create(
            data={
                # data to create a PostClassifier record
                'name': 'beeifcbebf',
                'prompt': 'bgcigfahea',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.PostClassifierCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple PostClassifier records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of PostClassifier record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await PostClassifier.prisma().create_many(
            data=[
                {
                    # data to create a PostClassifier record
                    'name': 'bcejgaggif',
                    'prompt': 'idfjadbcc',
                },
                {
                    # data to create a PostClassifier record
                    'name': 'hgdhbjhhj',
                    'prompt': 'ecjjjfbae',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.PostClassifierWhereUniqueInput,
        include: Optional[types.PostClassifierInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single PostClassifier record.

        Parameters
        ----------
        where
            PostClassifier filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned PostClassifier model

        Returns
        -------
        prisma.models.PostClassifier
            The deleted PostClassifier record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        postclassifier = await PostClassifier.prisma().delete(
            where={
                'id': 'bhhfibbigf',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.PostClassifierWhereUniqueInput,
        include: Optional[types.PostClassifierInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique PostClassifier record.

        Parameters
        ----------
        where
            PostClassifier filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PostClassifier model

        Returns
        -------
        prisma.models.PostClassifier
            The found PostClassifier record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        postclassifier = await PostClassifier.prisma().find_unique(
            where={
                'id': 'ijdbeffgg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.PostClassifierWhereUniqueInput,
        include: Optional[types.PostClassifierInclude] = None
    ) -> _PrismaModelT:
        """Find a unique PostClassifier record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            PostClassifier filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PostClassifier model

        Returns
        -------
        prisma.models.PostClassifier
            The found PostClassifier record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        postclassifier = await PostClassifier.prisma().find_unique_or_raise(
            where={
                'id': 'jjfeafhfj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PostClassifierWhereInput] = None,
        cursor: Optional[types.PostClassifierWhereUniqueInput] = None,
        include: Optional[types.PostClassifierInclude] = None,
        order: Optional[Union[types.PostClassifierOrderByInput, List[types.PostClassifierOrderByInput]]] = None,
        distinct: Optional[List[types.PostClassifierScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple PostClassifier records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of PostClassifier records returned
        skip
            Ignore the first N results
        where
            PostClassifier filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PostClassifier model
        order
            Order the returned PostClassifier records by any field
        distinct
            Filter PostClassifier records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.PostClassifier]
            The list of all PostClassifier records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 PostClassifier records
        postclassifiers = await PostClassifier.prisma().find_many(take=10)

        # find the first 5 PostClassifier records ordered by the name field
        postclassifiers = await PostClassifier.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PostClassifierWhereInput] = None,
        cursor: Optional[types.PostClassifierWhereUniqueInput] = None,
        include: Optional[types.PostClassifierInclude] = None,
        order: Optional[Union[types.PostClassifierOrderByInput, List[types.PostClassifierOrderByInput]]] = None,
        distinct: Optional[List[types.PostClassifierScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single PostClassifier record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PostClassifier filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PostClassifier model
        order
            Order the returned PostClassifier records by any field
        distinct
            Filter PostClassifier records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PostClassifier
            The first PostClassifier record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PostClassifier record ordered by the description field
        postclassifier = await PostClassifier.prisma().find_first(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PostClassifierWhereInput] = None,
        cursor: Optional[types.PostClassifierWhereUniqueInput] = None,
        include: Optional[types.PostClassifierInclude] = None,
        order: Optional[Union[types.PostClassifierOrderByInput, List[types.PostClassifierOrderByInput]]] = None,
        distinct: Optional[List[types.PostClassifierScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single PostClassifier record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PostClassifier filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PostClassifier model
        order
            Order the returned PostClassifier records by any field
        distinct
            Filter PostClassifier records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PostClassifier
            The first PostClassifier record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PostClassifier record ordered by the prompt field
        postclassifier = await PostClassifier.prisma().find_first_or_raise(
            skip=1,
            order={
                'prompt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.PostClassifierUpdateInput,
        where: types.PostClassifierWhereUniqueInput,
        include: Optional[types.PostClassifierInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single PostClassifier record.

        Parameters
        ----------
        data
            PostClassifier record data specifying what to update
        where
            PostClassifier filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned PostClassifier model

        Returns
        -------
        prisma.models.PostClassifier
            The updated PostClassifier record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        postclassifier = await PostClassifier.prisma().update(
            where={
                'id': 'cbachdgfce',
            },
            data={
                # data to update the PostClassifier record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.PostClassifierWhereUniqueInput,
        data: types.PostClassifierUpsertInput,
        include: Optional[types.PostClassifierInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            PostClassifier filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned PostClassifier model

        Returns
        -------
        prisma.models.PostClassifier
            The created or updated PostClassifier record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        postclassifier = await PostClassifier.prisma().upsert(
            where={
                'id': 'chbfcacbd',
            },
            data={
                'create': {
                    'id': 'chbfcacbd',
                    'name': 'hgdhbjhhj',
                    'prompt': 'ecjjjfbae',
                },
                'update': {
                    'name': 'hgdhbjhhj',
                    'prompt': 'ecjjjfbae',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.PostClassifierUpdateManyMutationInput,
        where: types.PostClassifierWhereInput,
    ) -> int:
        """Update multiple PostClassifier records

        Parameters
        ----------
        data
            PostClassifier data to update the selected PostClassifier records to
        where
            Filter to select the PostClassifier records to update

        Returns
        -------
        int
            The total number of PostClassifier records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all PostClassifier records
        total = await PostClassifier.prisma().update_many(
            data={
                'labels': ['efggddide']
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PostClassifierWhereInput] = None,
        cursor: Optional[types.PostClassifierWhereUniqueInput] = None,
    ) -> int:
        """Count the number of PostClassifier records present in the database

        Parameters
        ----------
        select
            Select the PostClassifier fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PostClassifier filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PostClassifierCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await PostClassifier.prisma().count()

        # results: prisma.types.PostClassifierCountAggregateOutput
        results = await PostClassifier.prisma().count(
            select={
                '_all': True,
                'examples': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.PostClassifierCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PostClassifierWhereInput] = None,
        cursor: Optional[types.PostClassifierWhereUniqueInput] = None,
    ) -> types.PostClassifierCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.PostClassifierCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PostClassifierWhereInput] = None,
        cursor: Optional[types.PostClassifierWhereUniqueInput] = None,
    ) -> Union[int, types.PostClassifierCountAggregateOutput]:
        """Count the number of PostClassifier records present in the database

        Parameters
        ----------
        select
            Select the PostClassifier fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PostClassifier filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PostClassifierCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await PostClassifier.prisma().count()

        # results: prisma.types.PostClassifierCountAggregateOutput
        results = await PostClassifier.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PostClassifierCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.PostClassifierWhereInput] = None
    ) -> int:
        """Delete multiple PostClassifier records.

        Parameters
        ----------
        where
            Optional PostClassifier filter to find the records to be deleted

        Returns
        -------
        int
            The total number of PostClassifier records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all PostClassifier records
        total = await PostClassifier.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.PostClassifierScalarFieldKeys'],
        *,
        where: Optional['types.PostClassifierWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PostClassifierAvgAggregateInput'] = None,
        sum: Optional['types.PostClassifierSumAggregateInput'] = None,
        min: Optional['types.PostClassifierMinAggregateInput'] = None,
        max: Optional['types.PostClassifierMaxAggregateInput'] = None,
        having: Optional['types.PostClassifierScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PostClassifierCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PostClassifierScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PostClassifierScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PostClassifierGroupByOutput']:
        """Group PostClassifier records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar PostClassifier fields to group records by
        where
            PostClassifier filter to select records
        take
            Limit the maximum number of PostClassifier records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PostClassifierGroupByOutput]
            A list of dictionaries representing the PostClassifier record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group PostClassifier records by updatedAt values
        # and count how many records are in each group
        results = await PostClassifier.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ChatAssetsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ChatAssets]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ChatAssets.prisma().query_raw(
            'SELECT * FROM ChatAssets WHERE chatId = $1',
            'caficfigfb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ChatAssets
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ChatAssets.prisma().query_first(
            'SELECT * FROM ChatAssets WHERE userId = $1',
            'bfidgijfjc',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ChatAssetsCreateInput,
        include: Optional[types.ChatAssetsInclude] = None
    ) -> _PrismaModelT:
        """Create a new ChatAssets record.

        Parameters
        ----------
        data
            ChatAssets record data
        include
            Specifies which relations should be loaded on the returned ChatAssets model

        Returns
        -------
        prisma.models.ChatAssets
            The created ChatAssets record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ChatAssets record from just the required fields
        chatassets = await ChatAssets.prisma().create(
            data={
                # data to create a ChatAssets record
                'chatId': 'ihieecagf',
                'asset': Json({'bghfciaafe': True}),
                'updatedAt': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ChatAssetsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ChatAssets records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ChatAssets record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ChatAssets.prisma().create_many(
            data=[
                {
                    # data to create a ChatAssets record
                    'chatId': 'bgchfhgceh',
                    'asset': Json({'cafeiaccbc': True}),
                    'updatedAt': datetime.datetime.utcnow(),
                },
                {
                    # data to create a ChatAssets record
                    'chatId': 'gaddfhfh',
                    'asset': Json({'gieegcbeg': True}),
                    'updatedAt': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ChatAssetsWhereUniqueInput,
        include: Optional[types.ChatAssetsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ChatAssets record.

        Parameters
        ----------
        where
            ChatAssets filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ChatAssets model

        Returns
        -------
        prisma.models.ChatAssets
            The deleted ChatAssets record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        chatassets = await ChatAssets.prisma().delete(
            where={
                'chatId': 'bgcffadich',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ChatAssetsWhereUniqueInput,
        include: Optional[types.ChatAssetsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ChatAssets record.

        Parameters
        ----------
        where
            ChatAssets filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ChatAssets model

        Returns
        -------
        prisma.models.ChatAssets
            The found ChatAssets record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        chatassets = await ChatAssets.prisma().find_unique(
            where={
                'chatId': 'fcbichhci',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ChatAssetsWhereUniqueInput,
        include: Optional[types.ChatAssetsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ChatAssets record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ChatAssets filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ChatAssets model

        Returns
        -------
        prisma.models.ChatAssets
            The found ChatAssets record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        chatassets = await ChatAssets.prisma().find_unique_or_raise(
            where={
                'chatId': 'bcggadccgf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ChatAssetsWhereInput] = None,
        cursor: Optional[types.ChatAssetsWhereUniqueInput] = None,
        include: Optional[types.ChatAssetsInclude] = None,
        order: Optional[Union[types.ChatAssetsOrderByInput, List[types.ChatAssetsOrderByInput]]] = None,
        distinct: Optional[List[types.ChatAssetsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ChatAssets records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ChatAssets records returned
        skip
            Ignore the first N results
        where
            ChatAssets filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ChatAssets model
        order
            Order the returned ChatAssets records by any field
        distinct
            Filter ChatAssets records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ChatAssets]
            The list of all ChatAssets records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ChatAssets records
        chatassets = await ChatAssets.prisma().find_many(take=10)

        # find the first 5 ChatAssets records ordered by the productId field
        chatassets = await ChatAssets.prisma().find_many(
            take=5,
            order={
                'productId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ChatAssetsWhereInput] = None,
        cursor: Optional[types.ChatAssetsWhereUniqueInput] = None,
        include: Optional[types.ChatAssetsInclude] = None,
        order: Optional[Union[types.ChatAssetsOrderByInput, List[types.ChatAssetsOrderByInput]]] = None,
        distinct: Optional[List[types.ChatAssetsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ChatAssets record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ChatAssets filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ChatAssets model
        order
            Order the returned ChatAssets records by any field
        distinct
            Filter ChatAssets records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ChatAssets
            The first ChatAssets record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ChatAssets record ordered by the asset field
        chatassets = await ChatAssets.prisma().find_first(
            skip=1,
            order={
                'asset': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ChatAssetsWhereInput] = None,
        cursor: Optional[types.ChatAssetsWhereUniqueInput] = None,
        include: Optional[types.ChatAssetsInclude] = None,
        order: Optional[Union[types.ChatAssetsOrderByInput, List[types.ChatAssetsOrderByInput]]] = None,
        distinct: Optional[List[types.ChatAssetsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ChatAssets record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ChatAssets filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ChatAssets model
        order
            Order the returned ChatAssets records by any field
        distinct
            Filter ChatAssets records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ChatAssets
            The first ChatAssets record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ChatAssets record ordered by the createdAt field
        chatassets = await ChatAssets.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ChatAssetsUpdateInput,
        where: types.ChatAssetsWhereUniqueInput,
        include: Optional[types.ChatAssetsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ChatAssets record.

        Parameters
        ----------
        data
            ChatAssets record data specifying what to update
        where
            ChatAssets filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ChatAssets model

        Returns
        -------
        prisma.models.ChatAssets
            The updated ChatAssets record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        chatassets = await ChatAssets.prisma().update(
            where={
                'chatId': 'jdcfdcgc',
            },
            data={
                # data to update the ChatAssets record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ChatAssetsWhereUniqueInput,
        data: types.ChatAssetsUpsertInput,
        include: Optional[types.ChatAssetsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ChatAssets filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ChatAssets model

        Returns
        -------
        prisma.models.ChatAssets
            The created or updated ChatAssets record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        chatassets = await ChatAssets.prisma().upsert(
            where={
                'chatId': 'cafdaehjid',
            },
            data={
                'create': {
                    'chatId': 'cafdaehjid',
                    'asset': Json({'gieegcbeg': True}),
                    'updatedAt': datetime.datetime.utcnow(),
                },
                'update': {
                    'asset': Json({'gieegcbeg': True}),
                    'updatedAt': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ChatAssetsUpdateManyMutationInput,
        where: types.ChatAssetsWhereInput,
    ) -> int:
        """Update multiple ChatAssets records

        Parameters
        ----------
        data
            ChatAssets data to update the selected ChatAssets records to
        where
            Filter to select the ChatAssets records to update

        Returns
        -------
        int
            The total number of ChatAssets records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ChatAssets records
        total = await ChatAssets.prisma().update_many(
            data={
                'updatedAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ChatAssetsWhereInput] = None,
        cursor: Optional[types.ChatAssetsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ChatAssets records present in the database

        Parameters
        ----------
        select
            Select the ChatAssets fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ChatAssets filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ChatAssetsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ChatAssets.prisma().count()

        # results: prisma.types.ChatAssetsCountAggregateOutput
        results = await ChatAssets.prisma().count(
            select={
                '_all': True,
                'chatId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ChatAssetsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ChatAssetsWhereInput] = None,
        cursor: Optional[types.ChatAssetsWhereUniqueInput] = None,
    ) -> types.ChatAssetsCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ChatAssetsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ChatAssetsWhereInput] = None,
        cursor: Optional[types.ChatAssetsWhereUniqueInput] = None,
    ) -> Union[int, types.ChatAssetsCountAggregateOutput]:
        """Count the number of ChatAssets records present in the database

        Parameters
        ----------
        select
            Select the ChatAssets fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ChatAssets filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ChatAssetsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ChatAssets.prisma().count()

        # results: prisma.types.ChatAssetsCountAggregateOutput
        results = await ChatAssets.prisma().count(
            select={
                '_all': True,
                'userId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ChatAssetsCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ChatAssetsWhereInput] = None
    ) -> int:
        """Delete multiple ChatAssets records.

        Parameters
        ----------
        where
            Optional ChatAssets filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ChatAssets records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ChatAssets records
        total = await ChatAssets.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ChatAssetsScalarFieldKeys'],
        *,
        where: Optional['types.ChatAssetsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ChatAssetsAvgAggregateInput'] = None,
        sum: Optional['types.ChatAssetsSumAggregateInput'] = None,
        min: Optional['types.ChatAssetsMinAggregateInput'] = None,
        max: Optional['types.ChatAssetsMaxAggregateInput'] = None,
        having: Optional['types.ChatAssetsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ChatAssetsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ChatAssetsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ChatAssetsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ChatAssetsGroupByOutput']:
        """Group ChatAssets records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ChatAssets fields to group records by
        where
            ChatAssets filter to select records
        take
            Limit the maximum number of ChatAssets records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ChatAssetsGroupByOutput]
            A list of dictionaries representing the ChatAssets record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ChatAssets records by productId values
        # and count how many records are in each group
        results = await ChatAssets.prisma().group_by(
            ['productId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class CustomCloneActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.CustomClone]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await CustomClone.prisma().query_raw(
            'SELECT * FROM CustomClone WHERE id = $1',
            'gifdddbia',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.CustomClone
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await CustomClone.prisma().query_first(
            'SELECT * FROM CustomClone WHERE userId = $1',
            'bchehecef',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.CustomCloneCreateInput,
        include: Optional[types.CustomCloneInclude] = None
    ) -> _PrismaModelT:
        """Create a new CustomClone record.

        Parameters
        ----------
        data
            CustomClone record data
        include
            Specifies which relations should be loaded on the returned CustomClone model

        Returns
        -------
        prisma.models.CustomClone
            The created CustomClone record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a CustomClone record from just the required fields
        customclone = await CustomClone.prisma().create(
            data={
                # data to create a CustomClone record
                'id': 'jeijcbhfe',
                'userId': 'bjgejjabff',
                'userEmail': 'bcciijbibg',
                'cloneName': 'cffcachfd',
                'metadata': Json({'bccdfhdigc': True}),
                'updatedAt': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.CustomCloneCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple CustomClone records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of CustomClone record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await CustomClone.prisma().create_many(
            data=[
                {
                    # data to create a CustomClone record
                    'id': 'febcgjbfj',
                    'userId': 'bageiegghg',
                    'userEmail': 'faidicegb',
                    'cloneName': 'bacecgfhbe',
                    'metadata': Json({'ihcahiead': True}),
                    'updatedAt': datetime.datetime.utcnow(),
                },
                {
                    # data to create a CustomClone record
                    'id': 'biheheiajg',
                    'userId': 'jbgijghgb',
                    'userEmail': 'hgjcghfbi',
                    'cloneName': 'icadbcehj',
                    'metadata': Json({'jchciaee': True}),
                    'updatedAt': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.CustomCloneWhereUniqueInput,
        include: Optional[types.CustomCloneInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single CustomClone record.

        Parameters
        ----------
        where
            CustomClone filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned CustomClone model

        Returns
        -------
        prisma.models.CustomClone
            The deleted CustomClone record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        customclone = await CustomClone.prisma().delete(
            where={
                'id': 'deeificjd',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.CustomCloneWhereUniqueInput,
        include: Optional[types.CustomCloneInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique CustomClone record.

        Parameters
        ----------
        where
            CustomClone filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned CustomClone model

        Returns
        -------
        prisma.models.CustomClone
            The found CustomClone record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        customclone = await CustomClone.prisma().find_unique(
            where={
                'id': 'bbcbhebbda',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.CustomCloneWhereUniqueInput,
        include: Optional[types.CustomCloneInclude] = None
    ) -> _PrismaModelT:
        """Find a unique CustomClone record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            CustomClone filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned CustomClone model

        Returns
        -------
        prisma.models.CustomClone
            The found CustomClone record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        customclone = await CustomClone.prisma().find_unique_or_raise(
            where={
                'id': 'bejfijgcfb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CustomCloneWhereInput] = None,
        cursor: Optional[types.CustomCloneWhereUniqueInput] = None,
        include: Optional[types.CustomCloneInclude] = None,
        order: Optional[Union[types.CustomCloneOrderByInput, List[types.CustomCloneOrderByInput]]] = None,
        distinct: Optional[List[types.CustomCloneScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple CustomClone records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of CustomClone records returned
        skip
            Ignore the first N results
        where
            CustomClone filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned CustomClone model
        order
            Order the returned CustomClone records by any field
        distinct
            Filter CustomClone records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.CustomClone]
            The list of all CustomClone records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 CustomClone records
        customclones = await CustomClone.prisma().find_many(take=10)

        # find the first 5 CustomClone records ordered by the userEmail field
        customclones = await CustomClone.prisma().find_many(
            take=5,
            order={
                'userEmail': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.CustomCloneWhereInput] = None,
        cursor: Optional[types.CustomCloneWhereUniqueInput] = None,
        include: Optional[types.CustomCloneInclude] = None,
        order: Optional[Union[types.CustomCloneOrderByInput, List[types.CustomCloneOrderByInput]]] = None,
        distinct: Optional[List[types.CustomCloneScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single CustomClone record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            CustomClone filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned CustomClone model
        order
            Order the returned CustomClone records by any field
        distinct
            Filter CustomClone records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.CustomClone
            The first CustomClone record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second CustomClone record ordered by the cloneName field
        customclone = await CustomClone.prisma().find_first(
            skip=1,
            order={
                'cloneName': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.CustomCloneWhereInput] = None,
        cursor: Optional[types.CustomCloneWhereUniqueInput] = None,
        include: Optional[types.CustomCloneInclude] = None,
        order: Optional[Union[types.CustomCloneOrderByInput, List[types.CustomCloneOrderByInput]]] = None,
        distinct: Optional[List[types.CustomCloneScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single CustomClone record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            CustomClone filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned CustomClone model
        order
            Order the returned CustomClone records by any field
        distinct
            Filter CustomClone records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.CustomClone
            The first CustomClone record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second CustomClone record ordered by the description field
        customclone = await CustomClone.prisma().find_first_or_raise(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.CustomCloneUpdateInput,
        where: types.CustomCloneWhereUniqueInput,
        include: Optional[types.CustomCloneInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single CustomClone record.

        Parameters
        ----------
        data
            CustomClone record data specifying what to update
        where
            CustomClone filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned CustomClone model

        Returns
        -------
        prisma.models.CustomClone
            The updated CustomClone record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        customclone = await CustomClone.prisma().update(
            where={
                'id': 'caifcbgii',
            },
            data={
                # data to update the CustomClone record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.CustomCloneWhereUniqueInput,
        data: types.CustomCloneUpsertInput,
        include: Optional[types.CustomCloneInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            CustomClone filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned CustomClone model

        Returns
        -------
        prisma.models.CustomClone
            The created or updated CustomClone record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        customclone = await CustomClone.prisma().upsert(
            where={
                'id': 'igaibbfgj',
            },
            data={
                'create': {
                    'id': 'igaibbfgj',
                    'userId': 'jbgijghgb',
                    'userEmail': 'hgjcghfbi',
                    'cloneName': 'icadbcehj',
                    'metadata': Json({'jchciaee': True}),
                    'updatedAt': datetime.datetime.utcnow(),
                },
                'update': {
                    'userId': 'jbgijghgb',
                    'userEmail': 'hgjcghfbi',
                    'cloneName': 'icadbcehj',
                    'metadata': Json({'jchciaee': True}),
                    'updatedAt': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.CustomCloneUpdateManyMutationInput,
        where: types.CustomCloneWhereInput,
    ) -> int:
        """Update multiple CustomClone records

        Parameters
        ----------
        data
            CustomClone data to update the selected CustomClone records to
        where
            Filter to select the CustomClone records to update

        Returns
        -------
        int
            The total number of CustomClone records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all CustomClone records
        total = await CustomClone.prisma().update_many(
            data={
                'category': 'bggajdcbbi'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CustomCloneWhereInput] = None,
        cursor: Optional[types.CustomCloneWhereUniqueInput] = None,
    ) -> int:
        """Count the number of CustomClone records present in the database

        Parameters
        ----------
        select
            Select the CustomClone fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            CustomClone filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.CustomCloneCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await CustomClone.prisma().count()

        # results: prisma.types.CustomCloneCountAggregateOutput
        results = await CustomClone.prisma().count(
            select={
                '_all': True,
                'metadata': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.CustomCloneCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CustomCloneWhereInput] = None,
        cursor: Optional[types.CustomCloneWhereUniqueInput] = None,
    ) -> types.CustomCloneCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.CustomCloneCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.CustomCloneWhereInput] = None,
        cursor: Optional[types.CustomCloneWhereUniqueInput] = None,
    ) -> Union[int, types.CustomCloneCountAggregateOutput]:
        """Count the number of CustomClone records present in the database

        Parameters
        ----------
        select
            Select the CustomClone fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            CustomClone filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.CustomCloneCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await CustomClone.prisma().count()

        # results: prisma.types.CustomCloneCountAggregateOutput
        results = await CustomClone.prisma().count(
            select={
                '_all': True,
                'keywords': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.CustomCloneCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.CustomCloneWhereInput] = None
    ) -> int:
        """Delete multiple CustomClone records.

        Parameters
        ----------
        where
            Optional CustomClone filter to find the records to be deleted

        Returns
        -------
        int
            The total number of CustomClone records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all CustomClone records
        total = await CustomClone.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.CustomCloneScalarFieldKeys'],
        *,
        where: Optional['types.CustomCloneWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.CustomCloneAvgAggregateInput'] = None,
        sum: Optional['types.CustomCloneSumAggregateInput'] = None,
        min: Optional['types.CustomCloneMinAggregateInput'] = None,
        max: Optional['types.CustomCloneMaxAggregateInput'] = None,
        having: Optional['types.CustomCloneScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.CustomCloneCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.CustomCloneScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.CustomCloneScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.CustomCloneGroupByOutput']:
        """Group CustomClone records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar CustomClone fields to group records by
        where
            CustomClone filter to select records
        take
            Limit the maximum number of CustomClone records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.CustomCloneGroupByOutput]
            A list of dictionaries representing the CustomClone record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group CustomClone records by profiles values
        # and count how many records are in each group
        results = await CustomClone.prisma().group_by(
            ['profiles'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class PreMadePromptActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.PreMadePrompt]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await PreMadePrompt.prisma().query_raw(
            'SELECT * FROM PreMadePrompt WHERE id = $1',
            'fcfhgbjed',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.PreMadePrompt
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await PreMadePrompt.prisma().query_first(
            'SELECT * FROM PreMadePrompt WHERE trigger = $1',
            'hdgcajhjg',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.PreMadePromptCreateInput,
        include: Optional[types.PreMadePromptInclude] = None
    ) -> _PrismaModelT:
        """Create a new PreMadePrompt record.

        Parameters
        ----------
        data
            PreMadePrompt record data
        include
            Specifies which relations should be loaded on the returned PreMadePrompt model

        Returns
        -------
        prisma.models.PreMadePrompt
            The created PreMadePrompt record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a PreMadePrompt record from just the required fields
        premadeprompt = await PreMadePrompt.prisma().create(
            data={
                # data to create a PreMadePrompt record
                'id': 'ejdjahicb',
                'trigger': 'gdjgigfgc',
                'name': 'gfeaahdeh',
                'description': 'bjafcgbffc',
                'prompt': 'hihegjif',
                'updatedAt': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.PreMadePromptCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple PreMadePrompt records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of PreMadePrompt record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await PreMadePrompt.prisma().create_many(
            data=[
                {
                    # data to create a PreMadePrompt record
                    'id': 'bdjidcidac',
                    'trigger': 'ifgaaagff',
                    'name': 'befcddgjce',
                    'description': 'bfhdbjjgfd',
                    'prompt': 'cabdjadaji',
                    'updatedAt': datetime.datetime.utcnow(),
                },
                {
                    # data to create a PreMadePrompt record
                    'id': 'faajgfadf',
                    'trigger': 'biaagcedjc',
                    'name': 'cahhaghecf',
                    'description': 'bghcbbcidi',
                    'prompt': 'jcgghhgdj',
                    'updatedAt': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.PreMadePromptWhereUniqueInput,
        include: Optional[types.PreMadePromptInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single PreMadePrompt record.

        Parameters
        ----------
        where
            PreMadePrompt filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned PreMadePrompt model

        Returns
        -------
        prisma.models.PreMadePrompt
            The deleted PreMadePrompt record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        premadeprompt = await PreMadePrompt.prisma().delete(
            where={
                'id': 'beehgcebbg',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.PreMadePromptWhereUniqueInput,
        include: Optional[types.PreMadePromptInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique PreMadePrompt record.

        Parameters
        ----------
        where
            PreMadePrompt filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PreMadePrompt model

        Returns
        -------
        prisma.models.PreMadePrompt
            The found PreMadePrompt record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        premadeprompt = await PreMadePrompt.prisma().find_unique(
            where={
                'id': 'bhdiaidiaf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.PreMadePromptWhereUniqueInput,
        include: Optional[types.PreMadePromptInclude] = None
    ) -> _PrismaModelT:
        """Find a unique PreMadePrompt record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            PreMadePrompt filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned PreMadePrompt model

        Returns
        -------
        prisma.models.PreMadePrompt
            The found PreMadePrompt record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        premadeprompt = await PreMadePrompt.prisma().find_unique_or_raise(
            where={
                'id': 'deajegcfi',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PreMadePromptWhereInput] = None,
        cursor: Optional[types.PreMadePromptWhereUniqueInput] = None,
        include: Optional[types.PreMadePromptInclude] = None,
        order: Optional[Union[types.PreMadePromptOrderByInput, List[types.PreMadePromptOrderByInput]]] = None,
        distinct: Optional[List[types.PreMadePromptScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple PreMadePrompt records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of PreMadePrompt records returned
        skip
            Ignore the first N results
        where
            PreMadePrompt filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PreMadePrompt model
        order
            Order the returned PreMadePrompt records by any field
        distinct
            Filter PreMadePrompt records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.PreMadePrompt]
            The list of all PreMadePrompt records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 PreMadePrompt records
        premadeprompts = await PreMadePrompt.prisma().find_many(take=10)

        # find the first 5 PreMadePrompt records ordered by the name field
        premadeprompts = await PreMadePrompt.prisma().find_many(
            take=5,
            order={
                'name': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PreMadePromptWhereInput] = None,
        cursor: Optional[types.PreMadePromptWhereUniqueInput] = None,
        include: Optional[types.PreMadePromptInclude] = None,
        order: Optional[Union[types.PreMadePromptOrderByInput, List[types.PreMadePromptOrderByInput]]] = None,
        distinct: Optional[List[types.PreMadePromptScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single PreMadePrompt record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PreMadePrompt filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PreMadePrompt model
        order
            Order the returned PreMadePrompt records by any field
        distinct
            Filter PreMadePrompt records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PreMadePrompt
            The first PreMadePrompt record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PreMadePrompt record ordered by the description field
        premadeprompt = await PreMadePrompt.prisma().find_first(
            skip=1,
            order={
                'description': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.PreMadePromptWhereInput] = None,
        cursor: Optional[types.PreMadePromptWhereUniqueInput] = None,
        include: Optional[types.PreMadePromptInclude] = None,
        order: Optional[Union[types.PreMadePromptOrderByInput, List[types.PreMadePromptOrderByInput]]] = None,
        distinct: Optional[List[types.PreMadePromptScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single PreMadePrompt record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            PreMadePrompt filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned PreMadePrompt model
        order
            Order the returned PreMadePrompt records by any field
        distinct
            Filter PreMadePrompt records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.PreMadePrompt
            The first PreMadePrompt record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second PreMadePrompt record ordered by the prompt field
        premadeprompt = await PreMadePrompt.prisma().find_first_or_raise(
            skip=1,
            order={
                'prompt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.PreMadePromptUpdateInput,
        where: types.PreMadePromptWhereUniqueInput,
        include: Optional[types.PreMadePromptInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single PreMadePrompt record.

        Parameters
        ----------
        data
            PreMadePrompt record data specifying what to update
        where
            PreMadePrompt filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned PreMadePrompt model

        Returns
        -------
        prisma.models.PreMadePrompt
            The updated PreMadePrompt record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        premadeprompt = await PreMadePrompt.prisma().update(
            where={
                'id': 'gabahhhjf',
            },
            data={
                # data to update the PreMadePrompt record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.PreMadePromptWhereUniqueInput,
        data: types.PreMadePromptUpsertInput,
        include: Optional[types.PreMadePromptInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            PreMadePrompt filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned PreMadePrompt model

        Returns
        -------
        prisma.models.PreMadePrompt
            The created or updated PreMadePrompt record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        premadeprompt = await PreMadePrompt.prisma().upsert(
            where={
                'id': 'cjagadcjg',
            },
            data={
                'create': {
                    'id': 'cjagadcjg',
                    'trigger': 'biaagcedjc',
                    'name': 'cahhaghecf',
                    'description': 'bghcbbcidi',
                    'prompt': 'jcgghhgdj',
                    'updatedAt': datetime.datetime.utcnow(),
                },
                'update': {
                    'trigger': 'biaagcedjc',
                    'name': 'cahhaghecf',
                    'description': 'bghcbbcidi',
                    'prompt': 'jcgghhgdj',
                    'updatedAt': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.PreMadePromptUpdateManyMutationInput,
        where: types.PreMadePromptWhereInput,
    ) -> int:
        """Update multiple PreMadePrompt records

        Parameters
        ----------
        data
            PreMadePrompt data to update the selected PreMadePrompt records to
        where
            Filter to select the PreMadePrompt records to update

        Returns
        -------
        int
            The total number of PreMadePrompt records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all PreMadePrompt records
        total = await PreMadePrompt.prisma().update_many(
            data={
                'active': False
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PreMadePromptWhereInput] = None,
        cursor: Optional[types.PreMadePromptWhereUniqueInput] = None,
    ) -> int:
        """Count the number of PreMadePrompt records present in the database

        Parameters
        ----------
        select
            Select the PreMadePrompt fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PreMadePrompt filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PreMadePromptCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await PreMadePrompt.prisma().count()

        # results: prisma.types.PreMadePromptCountAggregateOutput
        results = await PreMadePrompt.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.PreMadePromptCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PreMadePromptWhereInput] = None,
        cursor: Optional[types.PreMadePromptWhereUniqueInput] = None,
    ) -> types.PreMadePromptCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.PreMadePromptCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.PreMadePromptWhereInput] = None,
        cursor: Optional[types.PreMadePromptWhereUniqueInput] = None,
    ) -> Union[int, types.PreMadePromptCountAggregateOutput]:
        """Count the number of PreMadePrompt records present in the database

        Parameters
        ----------
        select
            Select the PreMadePrompt fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            PreMadePrompt filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.PreMadePromptCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await PreMadePrompt.prisma().count()

        # results: prisma.types.PreMadePromptCountAggregateOutput
        results = await PreMadePrompt.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.PreMadePromptCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.PreMadePromptWhereInput] = None
    ) -> int:
        """Delete multiple PreMadePrompt records.

        Parameters
        ----------
        where
            Optional PreMadePrompt filter to find the records to be deleted

        Returns
        -------
        int
            The total number of PreMadePrompt records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all PreMadePrompt records
        total = await PreMadePrompt.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.PreMadePromptScalarFieldKeys'],
        *,
        where: Optional['types.PreMadePromptWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.PreMadePromptAvgAggregateInput'] = None,
        sum: Optional['types.PreMadePromptSumAggregateInput'] = None,
        min: Optional['types.PreMadePromptMinAggregateInput'] = None,
        max: Optional['types.PreMadePromptMaxAggregateInput'] = None,
        having: Optional['types.PreMadePromptScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.PreMadePromptCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.PreMadePromptScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.PreMadePromptScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.PreMadePromptGroupByOutput']:
        """Group PreMadePrompt records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar PreMadePrompt fields to group records by
        where
            PreMadePrompt filter to select records
        take
            Limit the maximum number of PreMadePrompt records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.PreMadePromptGroupByOutput]
            A list of dictionaries representing the PreMadePrompt record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group PreMadePrompt records by id values
        # and count how many records are in each group
        results = await PreMadePrompt.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ScrapeJobActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ScrapeJob]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ScrapeJob.prisma().query_raw(
            'SELECT * FROM ScrapeJob WHERE id = $1',
            'bgbbaajbic',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ScrapeJob
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ScrapeJob.prisma().query_first(
            'SELECT * FROM ScrapeJob WHERE status = $1',
            'eegghdhjb',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ScrapeJobCreateInput,
        include: Optional[types.ScrapeJobInclude] = None
    ) -> _PrismaModelT:
        """Create a new ScrapeJob record.

        Parameters
        ----------
        data
            ScrapeJob record data
        include
            Specifies which relations should be loaded on the returned ScrapeJob model

        Returns
        -------
        prisma.models.ScrapeJob
            The created ScrapeJob record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ScrapeJob record from just the required fields
        scrapejob = await ScrapeJob.prisma().create(
            data={
                # data to create a ScrapeJob record
                'id': 'daafgidjg',
                'linkedinUrls': Json({'gdcgcgagj': True}),
                'maxPosts': 1724011690,
                'updatedAt': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ScrapeJobCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ScrapeJob records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ScrapeJob record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ScrapeJob.prisma().create_many(
            data=[
                {
                    # data to create a ScrapeJob record
                    'id': 'ehabfhegh',
                    'linkedinUrls': Json({'bcajcajjbc': True}),
                    'maxPosts': 1536744465,
                    'updatedAt': datetime.datetime.utcnow(),
                },
                {
                    # data to create a ScrapeJob record
                    'id': 'ececbijji',
                    'linkedinUrls': Json({'cbcfgdcdhf': True}),
                    'maxPosts': 536951780,
                    'updatedAt': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ScrapeJobWhereUniqueInput,
        include: Optional[types.ScrapeJobInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ScrapeJob record.

        Parameters
        ----------
        where
            ScrapeJob filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ScrapeJob model

        Returns
        -------
        prisma.models.ScrapeJob
            The deleted ScrapeJob record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        scrapejob = await ScrapeJob.prisma().delete(
            where={
                'id': 'jcehcdchh',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ScrapeJobWhereUniqueInput,
        include: Optional[types.ScrapeJobInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ScrapeJob record.

        Parameters
        ----------
        where
            ScrapeJob filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ScrapeJob model

        Returns
        -------
        prisma.models.ScrapeJob
            The found ScrapeJob record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        scrapejob = await ScrapeJob.prisma().find_unique(
            where={
                'id': 'bgcbjdhjcc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ScrapeJobWhereUniqueInput,
        include: Optional[types.ScrapeJobInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ScrapeJob record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ScrapeJob filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ScrapeJob model

        Returns
        -------
        prisma.models.ScrapeJob
            The found ScrapeJob record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        scrapejob = await ScrapeJob.prisma().find_unique_or_raise(
            where={
                'id': 'bieiidcabj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScrapeJobWhereInput] = None,
        cursor: Optional[types.ScrapeJobWhereUniqueInput] = None,
        include: Optional[types.ScrapeJobInclude] = None,
        order: Optional[Union[types.ScrapeJobOrderByInput, List[types.ScrapeJobOrderByInput]]] = None,
        distinct: Optional[List[types.ScrapeJobScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ScrapeJob records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ScrapeJob records returned
        skip
            Ignore the first N results
        where
            ScrapeJob filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ScrapeJob model
        order
            Order the returned ScrapeJob records by any field
        distinct
            Filter ScrapeJob records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ScrapeJob]
            The list of all ScrapeJob records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ScrapeJob records
        scrapejobs = await ScrapeJob.prisma().find_many(take=10)

        # find the first 5 ScrapeJob records ordered by the linkedinUrls field
        scrapejobs = await ScrapeJob.prisma().find_many(
            take=5,
            order={
                'linkedinUrls': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ScrapeJobWhereInput] = None,
        cursor: Optional[types.ScrapeJobWhereUniqueInput] = None,
        include: Optional[types.ScrapeJobInclude] = None,
        order: Optional[Union[types.ScrapeJobOrderByInput, List[types.ScrapeJobOrderByInput]]] = None,
        distinct: Optional[List[types.ScrapeJobScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ScrapeJob record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ScrapeJob filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ScrapeJob model
        order
            Order the returned ScrapeJob records by any field
        distinct
            Filter ScrapeJob records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ScrapeJob
            The first ScrapeJob record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ScrapeJob record ordered by the maxPosts field
        scrapejob = await ScrapeJob.prisma().find_first(
            skip=1,
            order={
                'maxPosts': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ScrapeJobWhereInput] = None,
        cursor: Optional[types.ScrapeJobWhereUniqueInput] = None,
        include: Optional[types.ScrapeJobInclude] = None,
        order: Optional[Union[types.ScrapeJobOrderByInput, List[types.ScrapeJobOrderByInput]]] = None,
        distinct: Optional[List[types.ScrapeJobScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ScrapeJob record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ScrapeJob filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ScrapeJob model
        order
            Order the returned ScrapeJob records by any field
        distinct
            Filter ScrapeJob records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ScrapeJob
            The first ScrapeJob record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ScrapeJob record ordered by the apifyRunId field
        scrapejob = await ScrapeJob.prisma().find_first_or_raise(
            skip=1,
            order={
                'apifyRunId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ScrapeJobUpdateInput,
        where: types.ScrapeJobWhereUniqueInput,
        include: Optional[types.ScrapeJobInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ScrapeJob record.

        Parameters
        ----------
        data
            ScrapeJob record data specifying what to update
        where
            ScrapeJob filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ScrapeJob model

        Returns
        -------
        prisma.models.ScrapeJob
            The updated ScrapeJob record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        scrapejob = await ScrapeJob.prisma().update(
            where={
                'id': 'bjcbfcieaa',
            },
            data={
                # data to update the ScrapeJob record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ScrapeJobWhereUniqueInput,
        data: types.ScrapeJobUpsertInput,
        include: Optional[types.ScrapeJobInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ScrapeJob filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ScrapeJob model

        Returns
        -------
        prisma.models.ScrapeJob
            The created or updated ScrapeJob record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        scrapejob = await ScrapeJob.prisma().upsert(
            where={
                'id': 'cbaaechiej',
            },
            data={
                'create': {
                    'id': 'cbaaechiej',
                    'linkedinUrls': Json({'cbcfgdcdhf': True}),
                    'maxPosts': 536951780,
                    'updatedAt': datetime.datetime.utcnow(),
                },
                'update': {
                    'linkedinUrls': Json({'cbcfgdcdhf': True}),
                    'maxPosts': 536951780,
                    'updatedAt': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ScrapeJobUpdateManyMutationInput,
        where: types.ScrapeJobWhereInput,
    ) -> int:
        """Update multiple ScrapeJob records

        Parameters
        ----------
        data
            ScrapeJob data to update the selected ScrapeJob records to
        where
            Filter to select the ScrapeJob records to update

        Returns
        -------
        int
            The total number of ScrapeJob records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ScrapeJob records
        total = await ScrapeJob.prisma().update_many(
            data={
                'result': Json({'iejbeaaeg': True})
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScrapeJobWhereInput] = None,
        cursor: Optional[types.ScrapeJobWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ScrapeJob records present in the database

        Parameters
        ----------
        select
            Select the ScrapeJob fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ScrapeJob filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ScrapeJobCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ScrapeJob.prisma().count()

        # results: prisma.types.ScrapeJobCountAggregateOutput
        results = await ScrapeJob.prisma().count(
            select={
                '_all': True,
                'error': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ScrapeJobCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScrapeJobWhereInput] = None,
        cursor: Optional[types.ScrapeJobWhereUniqueInput] = None,
    ) -> types.ScrapeJobCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ScrapeJobCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ScrapeJobWhereInput] = None,
        cursor: Optional[types.ScrapeJobWhereUniqueInput] = None,
    ) -> Union[int, types.ScrapeJobCountAggregateOutput]:
        """Count the number of ScrapeJob records present in the database

        Parameters
        ----------
        select
            Select the ScrapeJob fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ScrapeJob filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ScrapeJobCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ScrapeJob.prisma().count()

        # results: prisma.types.ScrapeJobCountAggregateOutput
        results = await ScrapeJob.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ScrapeJobCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ScrapeJobWhereInput] = None
    ) -> int:
        """Delete multiple ScrapeJob records.

        Parameters
        ----------
        where
            Optional ScrapeJob filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ScrapeJob records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ScrapeJob records
        total = await ScrapeJob.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ScrapeJobScalarFieldKeys'],
        *,
        where: Optional['types.ScrapeJobWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ScrapeJobAvgAggregateInput'] = None,
        sum: Optional['types.ScrapeJobSumAggregateInput'] = None,
        min: Optional['types.ScrapeJobMinAggregateInput'] = None,
        max: Optional['types.ScrapeJobMaxAggregateInput'] = None,
        having: Optional['types.ScrapeJobScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ScrapeJobCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ScrapeJobScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ScrapeJobScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ScrapeJobGroupByOutput']:
        """Group ScrapeJob records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ScrapeJob fields to group records by
        where
            ScrapeJob filter to select records
        take
            Limit the maximum number of ScrapeJob records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ScrapeJobGroupByOutput]
            A list of dictionaries representing the ScrapeJob record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ScrapeJob records by updatedAt values
        # and count how many records are in each group
        results = await ScrapeJob.prisma().group_by(
            ['updatedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class SearchQueryActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.SearchQuery]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await SearchQuery.prisma().query_raw(
            'SELECT * FROM SearchQuery WHERE id = $1',
            'jcibfcbhf',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.SearchQuery
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await SearchQuery.prisma().query_first(
            'SELECT * FROM SearchQuery WHERE filters = $1',
            Json({'chdadcaga': True}),
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.SearchQueryCreateInput,
        include: Optional[types.SearchQueryInclude] = None
    ) -> _PrismaModelT:
        """Create a new SearchQuery record.

        Parameters
        ----------
        data
            SearchQuery record data
        include
            Specifies which relations should be loaded on the returned SearchQuery model

        Returns
        -------
        prisma.models.SearchQuery
            The created SearchQuery record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a SearchQuery record from just the required fields
        searchquery = await SearchQuery.prisma().create(
            data={
                # data to create a SearchQuery record
                'id': 'jicieifbh',
                'filters': Json({'fbahdheji': True}),
                'sqlQuery': 'cbbheiicgh',
                'resultCount': 1401944936,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.SearchQueryCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple SearchQuery records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of SearchQuery record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await SearchQuery.prisma().create_many(
            data=[
                {
                    # data to create a SearchQuery record
                    'id': 'bcjhgahffd',
                    'filters': Json({'fbjeiiffa': True}),
                    'sqlQuery': 'jhgidcgbf',
                    'resultCount': 1696425492,
                },
                {
                    # data to create a SearchQuery record
                    'id': 'bgjcgchib',
                    'filters': Json({'bacdaibgfa': True}),
                    'sqlQuery': 'dchgibach',
                    'resultCount': 527748992,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.SearchQueryWhereUniqueInput,
        include: Optional[types.SearchQueryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single SearchQuery record.

        Parameters
        ----------
        where
            SearchQuery filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned SearchQuery model

        Returns
        -------
        prisma.models.SearchQuery
            The deleted SearchQuery record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        searchquery = await SearchQuery.prisma().delete(
            where={
                'id': 'cacjdfhejh',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.SearchQueryWhereUniqueInput,
        include: Optional[types.SearchQueryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique SearchQuery record.

        Parameters
        ----------
        where
            SearchQuery filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned SearchQuery model

        Returns
        -------
        prisma.models.SearchQuery
            The found SearchQuery record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        searchquery = await SearchQuery.prisma().find_unique(
            where={
                'id': 'bdbifjhbbi',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.SearchQueryWhereUniqueInput,
        include: Optional[types.SearchQueryInclude] = None
    ) -> _PrismaModelT:
        """Find a unique SearchQuery record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            SearchQuery filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned SearchQuery model

        Returns
        -------
        prisma.models.SearchQuery
            The found SearchQuery record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        searchquery = await SearchQuery.prisma().find_unique_or_raise(
            where={
                'id': 'cbccbbcdfb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SearchQueryWhereInput] = None,
        cursor: Optional[types.SearchQueryWhereUniqueInput] = None,
        include: Optional[types.SearchQueryInclude] = None,
        order: Optional[Union[types.SearchQueryOrderByInput, List[types.SearchQueryOrderByInput]]] = None,
        distinct: Optional[List[types.SearchQueryScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple SearchQuery records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of SearchQuery records returned
        skip
            Ignore the first N results
        where
            SearchQuery filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SearchQuery model
        order
            Order the returned SearchQuery records by any field
        distinct
            Filter SearchQuery records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.SearchQuery]
            The list of all SearchQuery records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 SearchQuery records
        searchquerys = await SearchQuery.prisma().find_many(take=10)

        # find the first 5 SearchQuery records ordered by the sqlQuery field
        searchquerys = await SearchQuery.prisma().find_many(
            take=5,
            order={
                'sqlQuery': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SearchQueryWhereInput] = None,
        cursor: Optional[types.SearchQueryWhereUniqueInput] = None,
        include: Optional[types.SearchQueryInclude] = None,
        order: Optional[Union[types.SearchQueryOrderByInput, List[types.SearchQueryOrderByInput]]] = None,
        distinct: Optional[List[types.SearchQueryScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single SearchQuery record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            SearchQuery filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SearchQuery model
        order
            Order the returned SearchQuery records by any field
        distinct
            Filter SearchQuery records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.SearchQuery
            The first SearchQuery record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second SearchQuery record ordered by the resultCount field
        searchquery = await SearchQuery.prisma().find_first(
            skip=1,
            order={
                'resultCount': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.SearchQueryWhereInput] = None,
        cursor: Optional[types.SearchQueryWhereUniqueInput] = None,
        include: Optional[types.SearchQueryInclude] = None,
        order: Optional[Union[types.SearchQueryOrderByInput, List[types.SearchQueryOrderByInput]]] = None,
        distinct: Optional[List[types.SearchQueryScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single SearchQuery record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            SearchQuery filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned SearchQuery model
        order
            Order the returned SearchQuery records by any field
        distinct
            Filter SearchQuery records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.SearchQuery
            The first SearchQuery record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second SearchQuery record ordered by the createdAt field
        searchquery = await SearchQuery.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.SearchQueryUpdateInput,
        where: types.SearchQueryWhereUniqueInput,
        include: Optional[types.SearchQueryInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single SearchQuery record.

        Parameters
        ----------
        data
            SearchQuery record data specifying what to update
        where
            SearchQuery filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned SearchQuery model

        Returns
        -------
        prisma.models.SearchQuery
            The updated SearchQuery record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        searchquery = await SearchQuery.prisma().update(
            where={
                'id': 'bacejedaca',
            },
            data={
                # data to update the SearchQuery record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.SearchQueryWhereUniqueInput,
        data: types.SearchQueryUpsertInput,
        include: Optional[types.SearchQueryInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            SearchQuery filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned SearchQuery model

        Returns
        -------
        prisma.models.SearchQuery
            The created or updated SearchQuery record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        searchquery = await SearchQuery.prisma().upsert(
            where={
                'id': 'bhbhdahfaj',
            },
            data={
                'create': {
                    'id': 'bhbhdahfaj',
                    'filters': Json({'bacdaibgfa': True}),
                    'sqlQuery': 'dchgibach',
                    'resultCount': 527748992,
                },
                'update': {
                    'filters': Json({'bacdaibgfa': True}),
                    'sqlQuery': 'dchgibach',
                    'resultCount': 527748992,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.SearchQueryUpdateManyMutationInput,
        where: types.SearchQueryWhereInput,
    ) -> int:
        """Update multiple SearchQuery records

        Parameters
        ----------
        data
            SearchQuery data to update the selected SearchQuery records to
        where
            Filter to select the SearchQuery records to update

        Returns
        -------
        int
            The total number of SearchQuery records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all SearchQuery records
        total = await SearchQuery.prisma().update_many(
            data={
                'id': 'bfjibceaec'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SearchQueryWhereInput] = None,
        cursor: Optional[types.SearchQueryWhereUniqueInput] = None,
    ) -> int:
        """Count the number of SearchQuery records present in the database

        Parameters
        ----------
        select
            Select the SearchQuery fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            SearchQuery filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SearchQueryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await SearchQuery.prisma().count()

        # results: prisma.types.SearchQueryCountAggregateOutput
        results = await SearchQuery.prisma().count(
            select={
                '_all': True,
                'filters': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.SearchQueryCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SearchQueryWhereInput] = None,
        cursor: Optional[types.SearchQueryWhereUniqueInput] = None,
    ) -> types.SearchQueryCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.SearchQueryCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.SearchQueryWhereInput] = None,
        cursor: Optional[types.SearchQueryWhereUniqueInput] = None,
    ) -> Union[int, types.SearchQueryCountAggregateOutput]:
        """Count the number of SearchQuery records present in the database

        Parameters
        ----------
        select
            Select the SearchQuery fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            SearchQuery filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.SearchQueryCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await SearchQuery.prisma().count()

        # results: prisma.types.SearchQueryCountAggregateOutput
        results = await SearchQuery.prisma().count(
            select={
                '_all': True,
                'sqlQuery': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.SearchQueryCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.SearchQueryWhereInput] = None
    ) -> int:
        """Delete multiple SearchQuery records.

        Parameters
        ----------
        where
            Optional SearchQuery filter to find the records to be deleted

        Returns
        -------
        int
            The total number of SearchQuery records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all SearchQuery records
        total = await SearchQuery.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.SearchQueryScalarFieldKeys'],
        *,
        where: Optional['types.SearchQueryWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.SearchQueryAvgAggregateInput'] = None,
        sum: Optional['types.SearchQuerySumAggregateInput'] = None,
        min: Optional['types.SearchQueryMinAggregateInput'] = None,
        max: Optional['types.SearchQueryMaxAggregateInput'] = None,
        having: Optional['types.SearchQueryScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.SearchQueryCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.SearchQueryScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.SearchQueryScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.SearchQueryGroupByOutput']:
        """Group SearchQuery records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar SearchQuery fields to group records by
        where
            SearchQuery filter to select records
        take
            Limit the maximum number of SearchQuery records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.SearchQueryGroupByOutput]
            A list of dictionaries representing the SearchQuery record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group SearchQuery records by resultCount values
        # and count how many records are in each group
        results = await SearchQuery.prisma().group_by(
            ['resultCount'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class StoryActionsActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.StoryActions]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await StoryActions.prisma().query_raw(
            'SELECT * FROM StoryActions WHERE storyId = $1',
            'ibhgcdbgd',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.StoryActions
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await StoryActions.prisma().query_first(
            'SELECT * FROM StoryActions WHERE actions = $1',
            Json({'badaffhddg': True}),
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.StoryActionsCreateInput,
        include: Optional[types.StoryActionsInclude] = None
    ) -> _PrismaModelT:
        """Create a new StoryActions record.

        Parameters
        ----------
        data
            StoryActions record data
        include
            Specifies which relations should be loaded on the returned StoryActions model

        Returns
        -------
        prisma.models.StoryActions
            The created StoryActions record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a StoryActions record from just the required fields
        storyactions = await StoryActions.prisma().create(
            data={
                # data to create a StoryActions record
                'storyId': 'bbdbfcfihd',
                'actions': Json({'cbagggbji': True}),
                'actionsUI': Json({'bchgafhjed': True}),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.StoryActionsCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple StoryActions records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of StoryActions record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await StoryActions.prisma().create_many(
            data=[
                {
                    # data to create a StoryActions record
                    'storyId': 'heffgjdei',
                    'actions': Json({'dahihgbeb': True}),
                    'actionsUI': Json({'bgheaejbcc': True}),
                },
                {
                    # data to create a StoryActions record
                    'storyId': 'bfcgifeged',
                    'actions': Json({'jfiahhbae': True}),
                    'actionsUI': Json({'bfbdafajcb': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.StoryActionsWhereUniqueInput,
        include: Optional[types.StoryActionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single StoryActions record.

        Parameters
        ----------
        where
            StoryActions filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned StoryActions model

        Returns
        -------
        prisma.models.StoryActions
            The deleted StoryActions record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        storyactions = await StoryActions.prisma().delete(
            where={
                'storyId': 'caeghehde',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.StoryActionsWhereUniqueInput,
        include: Optional[types.StoryActionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique StoryActions record.

        Parameters
        ----------
        where
            StoryActions filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StoryActions model

        Returns
        -------
        prisma.models.StoryActions
            The found StoryActions record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        storyactions = await StoryActions.prisma().find_unique(
            where={
                'storyId': 'caghgfbggd',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.StoryActionsWhereUniqueInput,
        include: Optional[types.StoryActionsInclude] = None
    ) -> _PrismaModelT:
        """Find a unique StoryActions record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            StoryActions filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StoryActions model

        Returns
        -------
        prisma.models.StoryActions
            The found StoryActions record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        storyactions = await StoryActions.prisma().find_unique_or_raise(
            where={
                'storyId': 'bbidjbbjaa',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StoryActionsWhereInput] = None,
        cursor: Optional[types.StoryActionsWhereUniqueInput] = None,
        include: Optional[types.StoryActionsInclude] = None,
        order: Optional[Union[types.StoryActionsOrderByInput, List[types.StoryActionsOrderByInput]]] = None,
        distinct: Optional[List[types.StoryActionsScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple StoryActions records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of StoryActions records returned
        skip
            Ignore the first N results
        where
            StoryActions filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StoryActions model
        order
            Order the returned StoryActions records by any field
        distinct
            Filter StoryActions records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.StoryActions]
            The list of all StoryActions records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 StoryActions records
        storyactions = await StoryActions.prisma().find_many(take=10)

        # find the first 5 StoryActions records ordered by the actionsUI field
        storyactions = await StoryActions.prisma().find_many(
            take=5,
            order={
                'actionsUI': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StoryActionsWhereInput] = None,
        cursor: Optional[types.StoryActionsWhereUniqueInput] = None,
        include: Optional[types.StoryActionsInclude] = None,
        order: Optional[Union[types.StoryActionsOrderByInput, List[types.StoryActionsOrderByInput]]] = None,
        distinct: Optional[List[types.StoryActionsScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single StoryActions record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StoryActions filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StoryActions model
        order
            Order the returned StoryActions records by any field
        distinct
            Filter StoryActions records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StoryActions
            The first StoryActions record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StoryActions record ordered by the storyId field
        storyactions = await StoryActions.prisma().find_first(
            skip=1,
            order={
                'storyId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StoryActionsWhereInput] = None,
        cursor: Optional[types.StoryActionsWhereUniqueInput] = None,
        include: Optional[types.StoryActionsInclude] = None,
        order: Optional[Union[types.StoryActionsOrderByInput, List[types.StoryActionsOrderByInput]]] = None,
        distinct: Optional[List[types.StoryActionsScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single StoryActions record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StoryActions filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StoryActions model
        order
            Order the returned StoryActions records by any field
        distinct
            Filter StoryActions records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StoryActions
            The first StoryActions record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StoryActions record ordered by the actions field
        storyactions = await StoryActions.prisma().find_first_or_raise(
            skip=1,
            order={
                'actions': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.StoryActionsUpdateInput,
        where: types.StoryActionsWhereUniqueInput,
        include: Optional[types.StoryActionsInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single StoryActions record.

        Parameters
        ----------
        data
            StoryActions record data specifying what to update
        where
            StoryActions filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned StoryActions model

        Returns
        -------
        prisma.models.StoryActions
            The updated StoryActions record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        storyactions = await StoryActions.prisma().update(
            where={
                'storyId': 'bfijhaejdd',
            },
            data={
                # data to update the StoryActions record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.StoryActionsWhereUniqueInput,
        data: types.StoryActionsUpsertInput,
        include: Optional[types.StoryActionsInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            StoryActions filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned StoryActions model

        Returns
        -------
        prisma.models.StoryActions
            The created or updated StoryActions record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        storyactions = await StoryActions.prisma().upsert(
            where={
                'storyId': 'bcedehfiji',
            },
            data={
                'create': {
                    'storyId': 'bcedehfiji',
                    'actions': Json({'jfiahhbae': True}),
                    'actionsUI': Json({'bfbdafajcb': True}),
                },
                'update': {
                    'actions': Json({'jfiahhbae': True}),
                    'actionsUI': Json({'bfbdafajcb': True}),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.StoryActionsUpdateManyMutationInput,
        where: types.StoryActionsWhereInput,
    ) -> int:
        """Update multiple StoryActions records

        Parameters
        ----------
        data
            StoryActions data to update the selected StoryActions records to
        where
            Filter to select the StoryActions records to update

        Returns
        -------
        int
            The total number of StoryActions records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all StoryActions records
        total = await StoryActions.prisma().update_many(
            data={
                'actionsUI': Json({'bdgjicijhb': True})
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StoryActionsWhereInput] = None,
        cursor: Optional[types.StoryActionsWhereUniqueInput] = None,
    ) -> int:
        """Count the number of StoryActions records present in the database

        Parameters
        ----------
        select
            Select the StoryActions fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StoryActions filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StoryActionsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await StoryActions.prisma().count()

        # results: prisma.types.StoryActionsCountAggregateOutput
        results = await StoryActions.prisma().count(
            select={
                '_all': True,
                'storyId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.StoryActionsCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StoryActionsWhereInput] = None,
        cursor: Optional[types.StoryActionsWhereUniqueInput] = None,
    ) -> types.StoryActionsCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.StoryActionsCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StoryActionsWhereInput] = None,
        cursor: Optional[types.StoryActionsWhereUniqueInput] = None,
    ) -> Union[int, types.StoryActionsCountAggregateOutput]:
        """Count the number of StoryActions records present in the database

        Parameters
        ----------
        select
            Select the StoryActions fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StoryActions filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StoryActionsCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await StoryActions.prisma().count()

        # results: prisma.types.StoryActionsCountAggregateOutput
        results = await StoryActions.prisma().count(
            select={
                '_all': True,
                'actions': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.StoryActionsCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.StoryActionsWhereInput] = None
    ) -> int:
        """Delete multiple StoryActions records.

        Parameters
        ----------
        where
            Optional StoryActions filter to find the records to be deleted

        Returns
        -------
        int
            The total number of StoryActions records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all StoryActions records
        total = await StoryActions.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.StoryActionsScalarFieldKeys'],
        *,
        where: Optional['types.StoryActionsWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.StoryActionsAvgAggregateInput'] = None,
        sum: Optional['types.StoryActionsSumAggregateInput'] = None,
        min: Optional['types.StoryActionsMinAggregateInput'] = None,
        max: Optional['types.StoryActionsMaxAggregateInput'] = None,
        having: Optional['types.StoryActionsScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.StoryActionsCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.StoryActionsScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.StoryActionsScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.StoryActionsGroupByOutput']:
        """Group StoryActions records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar StoryActions fields to group records by
        where
            StoryActions filter to select records
        take
            Limit the maximum number of StoryActions records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.StoryActionsGroupByOutput]
            A list of dictionaries representing the StoryActions record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group StoryActions records by actionsUI values
        # and count how many records are in each group
        results = await StoryActions.prisma().group_by(
            ['actionsUI'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class StoryCommentActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.StoryComment]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await StoryComment.prisma().query_raw(
            'SELECT * FROM StoryComment WHERE id = $1',
            'bghifjdeia',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.StoryComment
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await StoryComment.prisma().query_first(
            'SELECT * FROM StoryComment WHERE storyId = $1',
            'eadfcbbcb',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.StoryCommentCreateInput,
        include: Optional[types.StoryCommentInclude] = None
    ) -> _PrismaModelT:
        """Create a new StoryComment record.

        Parameters
        ----------
        data
            StoryComment record data
        include
            Specifies which relations should be loaded on the returned StoryComment model

        Returns
        -------
        prisma.models.StoryComment
            The created StoryComment record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a StoryComment record from just the required fields
        storycomment = await StoryComment.prisma().create(
            data={
                # data to create a StoryComment record
                'id': 'geihgahba',
                'storyId': 'gahdcdhbj',
                'userId': 'begiijahea',
                'comment': 'gcjadjaaf',
                'updatedAt': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.StoryCommentCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple StoryComment records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of StoryComment record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await StoryComment.prisma().create_many(
            data=[
                {
                    # data to create a StoryComment record
                    'id': 'bcbebgiaic',
                    'storyId': 'ijigbdcbj',
                    'userId': 'gfidhicai',
                    'comment': 'jfegcaafh',
                    'updatedAt': datetime.datetime.utcnow(),
                },
                {
                    # data to create a StoryComment record
                    'id': 'bcbeiajjfa',
                    'storyId': 'baehicaajf',
                    'userId': 'bdachdeiga',
                    'comment': 'ijdafccef',
                    'updatedAt': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.StoryCommentWhereUniqueInput,
        include: Optional[types.StoryCommentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single StoryComment record.

        Parameters
        ----------
        where
            StoryComment filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned StoryComment model

        Returns
        -------
        prisma.models.StoryComment
            The deleted StoryComment record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        storycomment = await StoryComment.prisma().delete(
            where={
                'id': 'ciaaiddag',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.StoryCommentWhereUniqueInput,
        include: Optional[types.StoryCommentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique StoryComment record.

        Parameters
        ----------
        where
            StoryComment filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StoryComment model

        Returns
        -------
        prisma.models.StoryComment
            The found StoryComment record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        storycomment = await StoryComment.prisma().find_unique(
            where={
                'id': 'fejggijff',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.StoryCommentWhereUniqueInput,
        include: Optional[types.StoryCommentInclude] = None
    ) -> _PrismaModelT:
        """Find a unique StoryComment record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            StoryComment filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned StoryComment model

        Returns
        -------
        prisma.models.StoryComment
            The found StoryComment record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        storycomment = await StoryComment.prisma().find_unique_or_raise(
            where={
                'id': 'hghjaaai',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StoryCommentWhereInput] = None,
        cursor: Optional[types.StoryCommentWhereUniqueInput] = None,
        include: Optional[types.StoryCommentInclude] = None,
        order: Optional[Union[types.StoryCommentOrderByInput, List[types.StoryCommentOrderByInput]]] = None,
        distinct: Optional[List[types.StoryCommentScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple StoryComment records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of StoryComment records returned
        skip
            Ignore the first N results
        where
            StoryComment filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StoryComment model
        order
            Order the returned StoryComment records by any field
        distinct
            Filter StoryComment records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.StoryComment]
            The list of all StoryComment records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 StoryComment records
        storycomments = await StoryComment.prisma().find_many(take=10)

        # find the first 5 StoryComment records ordered by the userId field
        storycomments = await StoryComment.prisma().find_many(
            take=5,
            order={
                'userId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StoryCommentWhereInput] = None,
        cursor: Optional[types.StoryCommentWhereUniqueInput] = None,
        include: Optional[types.StoryCommentInclude] = None,
        order: Optional[Union[types.StoryCommentOrderByInput, List[types.StoryCommentOrderByInput]]] = None,
        distinct: Optional[List[types.StoryCommentScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single StoryComment record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StoryComment filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StoryComment model
        order
            Order the returned StoryComment records by any field
        distinct
            Filter StoryComment records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StoryComment
            The first StoryComment record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StoryComment record ordered by the userEmail field
        storycomment = await StoryComment.prisma().find_first(
            skip=1,
            order={
                'userEmail': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.StoryCommentWhereInput] = None,
        cursor: Optional[types.StoryCommentWhereUniqueInput] = None,
        include: Optional[types.StoryCommentInclude] = None,
        order: Optional[Union[types.StoryCommentOrderByInput, List[types.StoryCommentOrderByInput]]] = None,
        distinct: Optional[List[types.StoryCommentScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single StoryComment record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            StoryComment filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned StoryComment model
        order
            Order the returned StoryComment records by any field
        distinct
            Filter StoryComment records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.StoryComment
            The first StoryComment record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second StoryComment record ordered by the userName field
        storycomment = await StoryComment.prisma().find_first_or_raise(
            skip=1,
            order={
                'userName': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.StoryCommentUpdateInput,
        where: types.StoryCommentWhereUniqueInput,
        include: Optional[types.StoryCommentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single StoryComment record.

        Parameters
        ----------
        data
            StoryComment record data specifying what to update
        where
            StoryComment filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned StoryComment model

        Returns
        -------
        prisma.models.StoryComment
            The updated StoryComment record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        storycomment = await StoryComment.prisma().update(
            where={
                'id': 'cajicjjdef',
            },
            data={
                # data to update the StoryComment record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.StoryCommentWhereUniqueInput,
        data: types.StoryCommentUpsertInput,
        include: Optional[types.StoryCommentInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            StoryComment filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned StoryComment model

        Returns
        -------
        prisma.models.StoryComment
            The created or updated StoryComment record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        storycomment = await StoryComment.prisma().upsert(
            where={
                'id': 'cefjaadec',
            },
            data={
                'create': {
                    'id': 'cefjaadec',
                    'storyId': 'baehicaajf',
                    'userId': 'bdachdeiga',
                    'comment': 'ijdafccef',
                    'updatedAt': datetime.datetime.utcnow(),
                },
                'update': {
                    'storyId': 'baehicaajf',
                    'userId': 'bdachdeiga',
                    'comment': 'ijdafccef',
                    'updatedAt': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.StoryCommentUpdateManyMutationInput,
        where: types.StoryCommentWhereInput,
    ) -> int:
        """Update multiple StoryComment records

        Parameters
        ----------
        data
            StoryComment data to update the selected StoryComment records to
        where
            Filter to select the StoryComment records to update

        Returns
        -------
        int
            The total number of StoryComment records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all StoryComment records
        total = await StoryComment.prisma().update_many(
            data={
                'comment': 'ibbigdigd'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StoryCommentWhereInput] = None,
        cursor: Optional[types.StoryCommentWhereUniqueInput] = None,
    ) -> int:
        """Count the number of StoryComment records present in the database

        Parameters
        ----------
        select
            Select the StoryComment fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StoryComment filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StoryCommentCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await StoryComment.prisma().count()

        # results: prisma.types.StoryCommentCountAggregateOutput
        results = await StoryComment.prisma().count(
            select={
                '_all': True,
                'createdAt': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.StoryCommentCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StoryCommentWhereInput] = None,
        cursor: Optional[types.StoryCommentWhereUniqueInput] = None,
    ) -> types.StoryCommentCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.StoryCommentCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.StoryCommentWhereInput] = None,
        cursor: Optional[types.StoryCommentWhereUniqueInput] = None,
    ) -> Union[int, types.StoryCommentCountAggregateOutput]:
        """Count the number of StoryComment records present in the database

        Parameters
        ----------
        select
            Select the StoryComment fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            StoryComment filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.StoryCommentCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await StoryComment.prisma().count()

        # results: prisma.types.StoryCommentCountAggregateOutput
        results = await StoryComment.prisma().count(
            select={
                '_all': True,
                'updatedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.StoryCommentCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.StoryCommentWhereInput] = None
    ) -> int:
        """Delete multiple StoryComment records.

        Parameters
        ----------
        where
            Optional StoryComment filter to find the records to be deleted

        Returns
        -------
        int
            The total number of StoryComment records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all StoryComment records
        total = await StoryComment.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.StoryCommentScalarFieldKeys'],
        *,
        where: Optional['types.StoryCommentWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.StoryCommentAvgAggregateInput'] = None,
        sum: Optional['types.StoryCommentSumAggregateInput'] = None,
        min: Optional['types.StoryCommentMinAggregateInput'] = None,
        max: Optional['types.StoryCommentMaxAggregateInput'] = None,
        having: Optional['types.StoryCommentScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.StoryCommentCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.StoryCommentScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.StoryCommentScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.StoryCommentGroupByOutput']:
        """Group StoryComment records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar StoryComment fields to group records by
        where
            StoryComment filter to select records
        take
            Limit the maximum number of StoryComment records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.StoryCommentGroupByOutput]
            A list of dictionaries representing the StoryComment record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group StoryComment records by id values
        # and count how many records are in each group
        results = await StoryComment.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class VapiCallConfigActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.VapiCallConfig]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await VapiCallConfig.prisma().query_raw(
            'SELECT * FROM VapiCallConfig WHERE id = $1',
            'bdiiiabbii',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.VapiCallConfig
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await VapiCallConfig.prisma().query_first(
            'SELECT * FROM VapiCallConfig WHERE sessionId = $1',
            'hfcfhhadh',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.VapiCallConfigCreateInput,
        include: Optional[types.VapiCallConfigInclude] = None
    ) -> _PrismaModelT:
        """Create a new VapiCallConfig record.

        Parameters
        ----------
        data
            VapiCallConfig record data
        include
            Specifies which relations should be loaded on the returned VapiCallConfig model

        Returns
        -------
        prisma.models.VapiCallConfig
            The created VapiCallConfig record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a VapiCallConfig record from just the required fields
        vapicallconfig = await VapiCallConfig.prisma().create(
            data={
                # data to create a VapiCallConfig record
                'id': 'bbihggdcji',
                'sessionId': 'hgjgibdgd',
                'config': Json({'bcbecjfice': True}),
                'expiresAt': datetime.datetime.utcnow(),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.VapiCallConfigCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple VapiCallConfig records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of VapiCallConfig record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await VapiCallConfig.prisma().create_many(
            data=[
                {
                    # data to create a VapiCallConfig record
                    'id': 'bacbebhjjd',
                    'sessionId': 'dfbfaddhe',
                    'config': Json({'bdcbbieibf': True}),
                    'expiresAt': datetime.datetime.utcnow(),
                },
                {
                    # data to create a VapiCallConfig record
                    'id': 'dgjhdcggi',
                    'sessionId': 'bbjbcdfabd',
                    'config': Json({'gchfgbcec': True}),
                    'expiresAt': datetime.datetime.utcnow(),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.VapiCallConfigWhereUniqueInput,
        include: Optional[types.VapiCallConfigInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single VapiCallConfig record.

        Parameters
        ----------
        where
            VapiCallConfig filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned VapiCallConfig model

        Returns
        -------
        prisma.models.VapiCallConfig
            The deleted VapiCallConfig record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        vapicallconfig = await VapiCallConfig.prisma().delete(
            where={
                'id': 'bihcjfcjah',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.VapiCallConfigWhereUniqueInput,
        include: Optional[types.VapiCallConfigInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique VapiCallConfig record.

        Parameters
        ----------
        where
            VapiCallConfig filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned VapiCallConfig model

        Returns
        -------
        prisma.models.VapiCallConfig
            The found VapiCallConfig record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        vapicallconfig = await VapiCallConfig.prisma().find_unique(
            where={
                'id': 'bhjdcicaii',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.VapiCallConfigWhereUniqueInput,
        include: Optional[types.VapiCallConfigInclude] = None
    ) -> _PrismaModelT:
        """Find a unique VapiCallConfig record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            VapiCallConfig filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned VapiCallConfig model

        Returns
        -------
        prisma.models.VapiCallConfig
            The found VapiCallConfig record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        vapicallconfig = await VapiCallConfig.prisma().find_unique_or_raise(
            where={
                'id': 'bibedjhcej',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VapiCallConfigWhereInput] = None,
        cursor: Optional[types.VapiCallConfigWhereUniqueInput] = None,
        include: Optional[types.VapiCallConfigInclude] = None,
        order: Optional[Union[types.VapiCallConfigOrderByInput, List[types.VapiCallConfigOrderByInput]]] = None,
        distinct: Optional[List[types.VapiCallConfigScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple VapiCallConfig records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of VapiCallConfig records returned
        skip
            Ignore the first N results
        where
            VapiCallConfig filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned VapiCallConfig model
        order
            Order the returned VapiCallConfig records by any field
        distinct
            Filter VapiCallConfig records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.VapiCallConfig]
            The list of all VapiCallConfig records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 VapiCallConfig records
        vapicallconfigs = await VapiCallConfig.prisma().find_many(take=10)

        # find the first 5 VapiCallConfig records ordered by the config field
        vapicallconfigs = await VapiCallConfig.prisma().find_many(
            take=5,
            order={
                'config': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.VapiCallConfigWhereInput] = None,
        cursor: Optional[types.VapiCallConfigWhereUniqueInput] = None,
        include: Optional[types.VapiCallConfigInclude] = None,
        order: Optional[Union[types.VapiCallConfigOrderByInput, List[types.VapiCallConfigOrderByInput]]] = None,
        distinct: Optional[List[types.VapiCallConfigScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single VapiCallConfig record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            VapiCallConfig filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned VapiCallConfig model
        order
            Order the returned VapiCallConfig records by any field
        distinct
            Filter VapiCallConfig records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.VapiCallConfig
            The first VapiCallConfig record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second VapiCallConfig record ordered by the createdAt field
        vapicallconfig = await VapiCallConfig.prisma().find_first(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.VapiCallConfigWhereInput] = None,
        cursor: Optional[types.VapiCallConfigWhereUniqueInput] = None,
        include: Optional[types.VapiCallConfigInclude] = None,
        order: Optional[Union[types.VapiCallConfigOrderByInput, List[types.VapiCallConfigOrderByInput]]] = None,
        distinct: Optional[List[types.VapiCallConfigScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single VapiCallConfig record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            VapiCallConfig filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned VapiCallConfig model
        order
            Order the returned VapiCallConfig records by any field
        distinct
            Filter VapiCallConfig records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.VapiCallConfig
            The first VapiCallConfig record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second VapiCallConfig record ordered by the expiresAt field
        vapicallconfig = await VapiCallConfig.prisma().find_first_or_raise(
            skip=1,
            order={
                'expiresAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.VapiCallConfigUpdateInput,
        where: types.VapiCallConfigWhereUniqueInput,
        include: Optional[types.VapiCallConfigInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single VapiCallConfig record.

        Parameters
        ----------
        data
            VapiCallConfig record data specifying what to update
        where
            VapiCallConfig filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned VapiCallConfig model

        Returns
        -------
        prisma.models.VapiCallConfig
            The updated VapiCallConfig record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        vapicallconfig = await VapiCallConfig.prisma().update(
            where={
                'id': 'bjcdajabfa',
            },
            data={
                # data to update the VapiCallConfig record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.VapiCallConfigWhereUniqueInput,
        data: types.VapiCallConfigUpsertInput,
        include: Optional[types.VapiCallConfigInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            VapiCallConfig filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned VapiCallConfig model

        Returns
        -------
        prisma.models.VapiCallConfig
            The created or updated VapiCallConfig record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        vapicallconfig = await VapiCallConfig.prisma().upsert(
            where={
                'id': 'bchhceeeff',
            },
            data={
                'create': {
                    'id': 'bchhceeeff',
                    'sessionId': 'bbjbcdfabd',
                    'config': Json({'gchfgbcec': True}),
                    'expiresAt': datetime.datetime.utcnow(),
                },
                'update': {
                    'sessionId': 'bbjbcdfabd',
                    'config': Json({'gchfgbcec': True}),
                    'expiresAt': datetime.datetime.utcnow(),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.VapiCallConfigUpdateManyMutationInput,
        where: types.VapiCallConfigWhereInput,
    ) -> int:
        """Update multiple VapiCallConfig records

        Parameters
        ----------
        data
            VapiCallConfig data to update the selected VapiCallConfig records to
        where
            Filter to select the VapiCallConfig records to update

        Returns
        -------
        int
            The total number of VapiCallConfig records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all VapiCallConfig records
        total = await VapiCallConfig.prisma().update_many(
            data={
                'id': 'bbgaifhdaa'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VapiCallConfigWhereInput] = None,
        cursor: Optional[types.VapiCallConfigWhereUniqueInput] = None,
    ) -> int:
        """Count the number of VapiCallConfig records present in the database

        Parameters
        ----------
        select
            Select the VapiCallConfig fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            VapiCallConfig filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.VapiCallConfigCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await VapiCallConfig.prisma().count()

        # results: prisma.types.VapiCallConfigCountAggregateOutput
        results = await VapiCallConfig.prisma().count(
            select={
                '_all': True,
                'sessionId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.VapiCallConfigCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VapiCallConfigWhereInput] = None,
        cursor: Optional[types.VapiCallConfigWhereUniqueInput] = None,
    ) -> types.VapiCallConfigCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.VapiCallConfigCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VapiCallConfigWhereInput] = None,
        cursor: Optional[types.VapiCallConfigWhereUniqueInput] = None,
    ) -> Union[int, types.VapiCallConfigCountAggregateOutput]:
        """Count the number of VapiCallConfig records present in the database

        Parameters
        ----------
        select
            Select the VapiCallConfig fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            VapiCallConfig filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.VapiCallConfigCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await VapiCallConfig.prisma().count()

        # results: prisma.types.VapiCallConfigCountAggregateOutput
        results = await VapiCallConfig.prisma().count(
            select={
                '_all': True,
                'config': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.VapiCallConfigCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.VapiCallConfigWhereInput] = None
    ) -> int:
        """Delete multiple VapiCallConfig records.

        Parameters
        ----------
        where
            Optional VapiCallConfig filter to find the records to be deleted

        Returns
        -------
        int
            The total number of VapiCallConfig records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all VapiCallConfig records
        total = await VapiCallConfig.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.VapiCallConfigScalarFieldKeys'],
        *,
        where: Optional['types.VapiCallConfigWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.VapiCallConfigAvgAggregateInput'] = None,
        sum: Optional['types.VapiCallConfigSumAggregateInput'] = None,
        min: Optional['types.VapiCallConfigMinAggregateInput'] = None,
        max: Optional['types.VapiCallConfigMaxAggregateInput'] = None,
        having: Optional['types.VapiCallConfigScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.VapiCallConfigCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.VapiCallConfigScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.VapiCallConfigScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.VapiCallConfigGroupByOutput']:
        """Group VapiCallConfig records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar VapiCallConfig fields to group records by
        where
            VapiCallConfig filter to select records
        take
            Limit the maximum number of VapiCallConfig records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.VapiCallConfigGroupByOutput]
            A list of dictionaries representing the VapiCallConfig record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group VapiCallConfig records by createdAt values
        # and count how many records are in each group
        results = await VapiCallConfig.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class VapiToolResultActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: 'Client', model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.VapiToolResult]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await VapiToolResult.prisma().query_raw(
            'SELECT * FROM VapiToolResult WHERE id = $1',
            'dgbcdaegb',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.VapiToolResult
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await VapiToolResult.prisma().query_first(
            'SELECT * FROM VapiToolResult WHERE sessionId = $1',
            'beagfbbjig',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.VapiToolResultCreateInput,
        include: Optional[types.VapiToolResultInclude] = None
    ) -> _PrismaModelT:
        """Create a new VapiToolResult record.

        Parameters
        ----------
        data
            VapiToolResult record data
        include
            Specifies which relations should be loaded on the returned VapiToolResult model

        Returns
        -------
        prisma.models.VapiToolResult
            The created VapiToolResult record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a VapiToolResult record from just the required fields
        vapitoolresult = await VapiToolResult.prisma().create(
            data={
                # data to create a VapiToolResult record
                'id': 'beicihhijb',
                'sessionId': 'fgggcdcjg',
                'query': 'ccjbbjigf',
                'raw': 'bhfaabbaha',
                'sanitized': 'ebajedhhf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.VapiToolResultCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple VapiToolResult records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of VapiToolResult record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await VapiToolResult.prisma().create_many(
            data=[
                {
                    # data to create a VapiToolResult record
                    'id': 'jajacedge',
                    'sessionId': 'hffgbabgf',
                    'query': 'biacbiieja',
                    'raw': 'cjejbgbff',
                    'sanitized': 'fgeahddae',
                },
                {
                    # data to create a VapiToolResult record
                    'id': 'diageigcf',
                    'sessionId': 'badagbgeha',
                    'query': 'ibgebbjch',
                    'raw': 'baieajjiee',
                    'sanitized': 'bahjhaccfd',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if self._client._active_provider == 'sqlite':
            raise errors.UnsupportedDatabaseError('sqlite', 'create_many()')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.VapiToolResultWhereUniqueInput,
        include: Optional[types.VapiToolResultInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single VapiToolResult record.

        Parameters
        ----------
        where
            VapiToolResult filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned VapiToolResult model

        Returns
        -------
        prisma.models.VapiToolResult
            The deleted VapiToolResult record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        vapitoolresult = await VapiToolResult.prisma().delete(
            where={
                'id': 'hffhfabhi',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.VapiToolResultWhereUniqueInput,
        include: Optional[types.VapiToolResultInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique VapiToolResult record.

        Parameters
        ----------
        where
            VapiToolResult filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned VapiToolResult model

        Returns
        -------
        prisma.models.VapiToolResult
            The found VapiToolResult record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        vapitoolresult = await VapiToolResult.prisma().find_unique(
            where={
                'id': 'bbcigiadhb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.VapiToolResultWhereUniqueInput,
        include: Optional[types.VapiToolResultInclude] = None
    ) -> _PrismaModelT:
        """Find a unique VapiToolResult record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            VapiToolResult filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned VapiToolResult model

        Returns
        -------
        prisma.models.VapiToolResult
            The found VapiToolResult record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        vapitoolresult = await VapiToolResult.prisma().find_unique_or_raise(
            where={
                'id': 'cfjagbbae',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VapiToolResultWhereInput] = None,
        cursor: Optional[types.VapiToolResultWhereUniqueInput] = None,
        include: Optional[types.VapiToolResultInclude] = None,
        order: Optional[Union[types.VapiToolResultOrderByInput, List[types.VapiToolResultOrderByInput]]] = None,
        distinct: Optional[List[types.VapiToolResultScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple VapiToolResult records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of VapiToolResult records returned
        skip
            Ignore the first N results
        where
            VapiToolResult filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned VapiToolResult model
        order
            Order the returned VapiToolResult records by any field
        distinct
            Filter VapiToolResult records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.VapiToolResult]
            The list of all VapiToolResult records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 VapiToolResult records
        vapitoolresults = await VapiToolResult.prisma().find_many(take=10)

        # find the first 5 VapiToolResult records ordered by the query field
        vapitoolresults = await VapiToolResult.prisma().find_many(
            take=5,
            order={
                'query': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.VapiToolResultWhereInput] = None,
        cursor: Optional[types.VapiToolResultWhereUniqueInput] = None,
        include: Optional[types.VapiToolResultInclude] = None,
        order: Optional[Union[types.VapiToolResultOrderByInput, List[types.VapiToolResultOrderByInput]]] = None,
        distinct: Optional[List[types.VapiToolResultScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single VapiToolResult record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            VapiToolResult filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned VapiToolResult model
        order
            Order the returned VapiToolResult records by any field
        distinct
            Filter VapiToolResult records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.VapiToolResult
            The first VapiToolResult record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second VapiToolResult record ordered by the raw field
        vapitoolresult = await VapiToolResult.prisma().find_first(
            skip=1,
            order={
                'raw': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.VapiToolResultWhereInput] = None,
        cursor: Optional[types.VapiToolResultWhereUniqueInput] = None,
        include: Optional[types.VapiToolResultInclude] = None,
        order: Optional[Union[types.VapiToolResultOrderByInput, List[types.VapiToolResultOrderByInput]]] = None,
        distinct: Optional[List[types.VapiToolResultScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single VapiToolResult record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            VapiToolResult filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned VapiToolResult model
        order
            Order the returned VapiToolResult records by any field
        distinct
            Filter VapiToolResult records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.VapiToolResult
            The first VapiToolResult record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second VapiToolResult record ordered by the sanitized field
        vapitoolresult = await VapiToolResult.prisma().find_first_or_raise(
            skip=1,
            order={
                'sanitized': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.VapiToolResultUpdateInput,
        where: types.VapiToolResultWhereUniqueInput,
        include: Optional[types.VapiToolResultInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single VapiToolResult record.

        Parameters
        ----------
        data
            VapiToolResult record data specifying what to update
        where
            VapiToolResult filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned VapiToolResult model

        Returns
        -------
        prisma.models.VapiToolResult
            The updated VapiToolResult record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        vapitoolresult = await VapiToolResult.prisma().update(
            where={
                'id': 'bbbfhdidef',
            },
            data={
                # data to update the VapiToolResult record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.VapiToolResultWhereUniqueInput,
        data: types.VapiToolResultUpsertInput,
        include: Optional[types.VapiToolResultInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            VapiToolResult filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned VapiToolResult model

        Returns
        -------
        prisma.models.VapiToolResult
            The created or updated VapiToolResult record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        vapitoolresult = await VapiToolResult.prisma().upsert(
            where={
                'id': 'bdadhibhec',
            },
            data={
                'create': {
                    'id': 'bdadhibhec',
                    'sessionId': 'badagbgeha',
                    'query': 'ibgebbjch',
                    'raw': 'baieajjiee',
                    'sanitized': 'bahjhaccfd',
                },
                'update': {
                    'sessionId': 'badagbgeha',
                    'query': 'ibgebbjch',
                    'raw': 'baieajjiee',
                    'sanitized': 'bahjhaccfd',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.VapiToolResultUpdateManyMutationInput,
        where: types.VapiToolResultWhereInput,
    ) -> int:
        """Update multiple VapiToolResult records

        Parameters
        ----------
        data
            VapiToolResult data to update the selected VapiToolResult records to
        where
            Filter to select the VapiToolResult records to update

        Returns
        -------
        int
            The total number of VapiToolResult records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all VapiToolResult records
        total = await VapiToolResult.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VapiToolResultWhereInput] = None,
        cursor: Optional[types.VapiToolResultWhereUniqueInput] = None,
    ) -> int:
        """Count the number of VapiToolResult records present in the database

        Parameters
        ----------
        select
            Select the VapiToolResult fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            VapiToolResult filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.VapiToolResultCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await VapiToolResult.prisma().count()

        # results: prisma.types.VapiToolResultCountAggregateOutput
        results = await VapiToolResult.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.VapiToolResultCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VapiToolResultWhereInput] = None,
        cursor: Optional[types.VapiToolResultWhereUniqueInput] = None,
    ) -> types.VapiToolResultCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.VapiToolResultCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.VapiToolResultWhereInput] = None,
        cursor: Optional[types.VapiToolResultWhereUniqueInput] = None,
    ) -> Union[int, types.VapiToolResultCountAggregateOutput]:
        """Count the number of VapiToolResult records present in the database

        Parameters
        ----------
        select
            Select the VapiToolResult fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            VapiToolResult filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.VapiToolResultCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await VapiToolResult.prisma().count()

        # results: prisma.types.VapiToolResultCountAggregateOutput
        results = await VapiToolResult.prisma().count(
            select={
                '_all': True,
                'sessionId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.VapiToolResultCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.VapiToolResultWhereInput] = None
    ) -> int:
        """Delete multiple VapiToolResult records.

        Parameters
        ----------
        where
            Optional VapiToolResult filter to find the records to be deleted

        Returns
        -------
        int
            The total number of VapiToolResult records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all VapiToolResult records
        total = await VapiToolResult.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.VapiToolResultScalarFieldKeys'],
        *,
        where: Optional['types.VapiToolResultWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.VapiToolResultAvgAggregateInput'] = None,
        sum: Optional['types.VapiToolResultSumAggregateInput'] = None,
        min: Optional['types.VapiToolResultMinAggregateInput'] = None,
        max: Optional['types.VapiToolResultMaxAggregateInput'] = None,
        having: Optional['types.VapiToolResultScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.VapiToolResultCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.VapiToolResultScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.VapiToolResultScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.VapiToolResultGroupByOutput']:
        """Group VapiToolResult records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar VapiToolResult fields to group records by
        where
            VapiToolResult filter to select records
        take
            Limit the maximum number of VapiToolResult records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.VapiToolResultGroupByOutput]
            A list of dictionaries representing the VapiToolResult record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group VapiToolResult records by query values
        # and count how many records are in each group
        results = await VapiToolResult.prisma().group_by(
            ['query'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models