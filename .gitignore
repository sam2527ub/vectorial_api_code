__pycache__/
*.pyc

# Secrets and local env (never commit)
.env
.env.*
.env*.local
**/.env
**/.env.*

# Setup docs that may contain API keys/tokens
VERCEL_SETUP.md
**/VERCEL_SETUP.md

# One-off scripts that previously had hardcoded API keys
upload_summary_prompts_safe.py

venv/
.venv/

# Local-only theme discovery I/O (CLI defaults); production uses S3 + theme_category_discovery
local/

# Generated Prisma clients (regenerated from schemas)
prisma_client/
audience_prisma_client/
prisma/prisma_client/
prisma/audience_prisma_client/

# AWS/CDK specific files (not needed for Vercel deployment)
cdk/
cdk-outputs.json
deploy-aws.sh
Dockerfile

node_modules/

# App subprojects (local / separate repos)
apps/

# Reddit Vercel workflow (local copy)
reddit-vercel-worflow/

.vercel

# Local prediction artifacts (i0 / run outputs)
Predictions/

# Local LinkedIn / SGO script trees (large data + experiments; keep on disk only)
scripts/scripts_sgo/
scripts/Linkedin_Posts_Extraction/
scripts/LInkedin_Category_Topic_Extraction/
scripts/Linkedin_Category_Topic_Extraction/