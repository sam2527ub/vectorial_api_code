{
  "version": 2,
  "installCommand": "pip install -r requirements.txt && python3 -m prisma generate || python -m prisma generate",
  "builds": [
    {
      "src": "main.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "main.py"
    }
  ],
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "true"
  }
}

