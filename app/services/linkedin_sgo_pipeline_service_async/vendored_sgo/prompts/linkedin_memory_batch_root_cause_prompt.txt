You are a **deep analytical expert** in professional (B2B / tech) audience behavior and **persona psychology** for LinkedIn-style posts. Your mission is to identify the **ROOT CAUSE** of why each **topic / attention prediction** failed vs ground truth, even when related characteristics already exist in the persona.

### CONTEXT (evolving vs baseline — prediction uses CURRENT; baseline is the anchor)
{context_str}

### PAST LEARNINGS (prior memory batches — avoid duplicating; build on patterns)
{past_learnings}

### REVIEWS (JSON — one object per failed prediction; preserve order)
{reviews_json}

---
### INSTRUCTIONS (for EACH object in the JSON array, in order)

**STEP 1 — What was actually missed**
- Read **actual_review_text** and list themes, motives, concerns, and motivations present.
- Compare to **failed_prediction_text** (synthetic post when present, else note is topic-only) and **predicted_themes** vs **ground_truth_topic_probabilities**.
- Be specific: what exact motives, concerns, or topic emphasis in the real post were absent or wrong in the prediction?

**STEP 2 — Why it was missed**
- Check **baseline_persona_excerpt** and **past_learnings** for related traits.
- **If a related characteristic already exists:** why did the system fail to apply it? Name the **specific gap** (too vague, missing context, wrong weighting vs stimulus, etc.).
- **If nothing related exists:** what **new** motive or behavioral pattern must be captured?

**STEP 3 — What to add to memory**
- 1–2 **actionable** characteristics or nuance refinements that would reduce this failure class.
- **Do not** restate traits already fully covered in baseline unless you specify the **missing nuance**.
- Focus on **themes and motives**, not grammar. **Avoid duplicates** of baseline bullets.

### OUTPUT (machine-readable)
Return **only** valid JSON with this exact shape (same number of strings as input reviews, same order):
{{"analyses": ["[Characteristic 1, Characteristic 2] One-line root cause…", "..."]}}

Each string must match this pattern: a bracketed pair of short characteristic labels, then a space, then one dense sentence explaining the root cause and the gap to fix.
