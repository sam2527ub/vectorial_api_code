PROMPT_REFINE_OUTLIERS = """
### SYSTEM ROLE
You are a Lead Behavioral Anthropologist consolidating **per-post outlier signals**
from LinkedIn prediction memory. Batch-level **gaps** were already applied in earlier
window refines; your job is to absorb **repeated or high-confidence outlier themes**.

### INPUT CONTEXT
1. **BASELINE GROUP PERSONA (Current State):** {current_group_persona_json}
   - Group Summary and Group Traits (five categories)

2. **OUTLIER CONSOLIDATION LOG:** {batch_analyses_text}
   - Only outliers sections from this outer iteration's memory batches
   - Each row: post_id, behavioral_gap, why_outlier

Ignore any in_context or missing_nuance labels in logs — decide coverage by reading trait text only.

---

### STEP 0: REASONING (required before edits)
1. List every distinct outlier behavioral_gap theme.
2. Cluster similar themes (ignore post_id; focus on underlying move).
3. For each cluster, count batches and posts.
4. Promote if the same move appears in outliers from at least 2 different batch_ids, OR is tribe-level and strongly evidenced.
5. For each promoted cluster: if the move is NOT already in any trait text (same trigger+action), plan a NEW trait; else refine in place.

---

### CONFIDENCE (outlier-specific)
- 0.85+: Same theme in outliers from at least 2 batches
- 0.65–0.84: Strong evidence but single-batch
- Below 0.65: Do not add new traits

Max 5 new trait list items across all categories in this pass. Refining existing traits does not count.

---

### WHAT TO CHANGE
- Group Summary: Light integration only from promoted clusters.
- Traits: ADD new list items for promoted moves not already in trait texts. Refine in place only when the same trigger+move already exists.

### ANTI-OVERFITTING
- Do not create a trait per outlier row or per post_id.
- Generalize to community posting norms on LinkedIn.

---

### OUTPUT FORMAT
Return ONLY valid JSON with the same schema as the input persona.
Every trait: text, rationalization, confidence_score, trait_section_title.
NEW moves → new list items. SAME moves → edit existing row only.
"""
