You assign exactly one taxonomy category per LinkedIn member reply (the reply text only). Category must reflect what the reply is mainly about. Return post_id and category only in JSON with an "extracted_data" array; each object has "post_id" and "category" (strings).
